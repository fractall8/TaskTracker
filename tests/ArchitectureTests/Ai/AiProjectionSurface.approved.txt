# APPROVED AI EXPOSURE SURFACE
#
# Every line below is a field the language model can read. This file is the audit artifact for
# EPIC 3 Decision 14: a reviewer should be able to approve or reject a change to what the AI sees
# by reading the diff of this file alone.
#
# Format:  TypeName.PropertyName : PropertyType     (sorted, set semantics — order is not tracked)
#
# Before adding a line, check it against EPIC 3 §6. Never approve:
#   - any user name, email, avatar, or user identifier (AssigneeId, ReporterId, *ById, ...)
#   - task/board/workspace Description, comment bodies, attachment names
#   - a role property not prefixed My*
#
# Empty until Story 3.2 introduces Application.Ai.Projections. The guards are proven to work today by
# AiProjectionRuleSelfTests, which runs the same rules against deliberately-broken fixtures.
