---
baseline_commit: ef13ebc9f9710561a83421bfbc0ae7f35bbbb0e5
---

# Story 1.1: Real-Time Board Calls — Azure Communication Services Video & Screen Share

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **Revision note:** This story was originally specced around a custom P2P WebRTC mesh (dedicated `BoardCallHub`, hand-rolled SDP/ICE signaling, STUN/TURN). It has been **pivoted to Azure Communication Services (ACS)** — see Change Log. Tasks 1–2 are now fully implemented for the ACS design (including the `AcsRoomId`/`AcsCommunicationUserId` additions). Tasks 3–8 are rewritten from scratch for ACS and have not been implemented.

## Story

As a board member,
I want to start or join a real-time video call with screen sharing scoped to my board,
so that my team can run synchronous collaboration (e.g. Daily Scrum) without leaving the board.

## Acceptance Criteria

1. A **ScrumMaster or Admin** board member can start a call on a Board that has no active call (enforced at the database level via the existing partial unique index on `BoardCalls.BoardId WHERE "EndedAt" IS NULL`). Starting provisions an **Azure Communication Services Room** and returns the starter an ACS access token scoped to that room.
2. Any board member can see an active-call indicator ("Call in progress · N/4 joined") and join, up to a cap of **4 total participants**. The cap is a single centralized constant (not scattered across the codebase) so it can be swapped for a per-subscription-plan limit later without restructuring — see Dev Notes §7. A 5th join attempt fails with a clear "call is full" error.
3. Joining adds the user as an **ACS Room participant** — role mapped from their `BoardRoleDto` (`ScrumMaster`/`Admin` → ACS `Presenter`, `User` → ACS `Attendee`) — and returns them an ACS access token. The Blazor frontend uses the **`@azure/communication-calling`** JS SDK (via JS interop) to connect audio/video/screen-share. **No raw `RTCPeerConnection`, SDP, or ICE candidate handling exists anywhere in this codebase** — ACS owns that entirely.
4. If a user denies camera/microphone permission, or has no such device, they can still join **audio-only or view-only** rather than being blocked from joining entirely.
5. Participant join/leave state in **our** database is driven **exclusively by Azure Event Grid webhooks** (`Microsoft.Communication.CallParticipantAdded` / `CallParticipantRemoved`) relayed from the ACS Room's call — never written directly by a client-triggered REST call. When the last confirmed participant leaves (per the webhook), the call is automatically marked ended.
6. A ScrumMaster/Admin can explicitly **end the call for everyone** at any time; this also revokes ACS Room participant access so no one can silently continue the call afterward.
7. Starting, participant changes, and ending a call all notify board members via the **existing** `BoardActionsHub`/`BoardActionNotification` channel — **no new SignalR hub exists for this feature.**

## Tasks / Subtasks

- [x] **Task 1 — Domain & Persistence** (AC: 1, 2, 5, 6) — *fully implemented, including the ACS pivot additions (`AcsRoomId`, `AcsCommunicationUserId`)*
  - [x] Add `Domain/Entities/BoardCall.cs` and `Domain/Entities/BoardCallParticipant.cs`
  - [x] Add `Persistence/Configurations/BoardCallConfiguration.cs` and `BoardCallParticipantConfiguration.cs`
  - [x] Add `Database/Scripts/0020_CreateBoardCallTables.sql` — two tables + FKs + the two partial unique indexes (one active call per board, one active participant per user per call)
  - [x] Add `Application/Interfaces/Repositories/IBoardCallRepository.cs` + `Persistence/Repositories/BoardCallRepository.cs`
  - [x] Register the new repository in `RepositoriesModule`
  - [x] **NEW — add `AcsRoomId` (required `string`) to `BoardCall`.** Since `0020_CreateBoardCallTables.sql` has not been applied to any shared database yet (this session only), **amend it in place** rather than adding a new script: add `"AcsRoomId" character varying(128) NOT NULL` to the `BoardCalls` table definition, plus `CREATE UNIQUE INDEX "UX_BoardCalls_AcsRoomId" ON "BoardCalls" ("AcsRoomId");` (one ACS Room maps 1:1 to one `BoardCall`). Update `BoardCallConfiguration.cs` (`builder.Property(c => c.AcsRoomId).IsRequired().HasMaxLength(128);`) to match.
  - [x] **NEW — persist one ACS identity per `User`.** Add `public string? AcsCommunicationUserId { get; set; }` to `Domain/Entities/User.cs`. Unlike `BoardCalls`, the `Users` table already exists and has been through prior migrations — this **must** be a new script, `Database/Scripts/0021_AddAcsCommunicationUserIdToUsers.sql`:
    ```sql
    BEGIN;

    ALTER TABLE "Users" ADD COLUMN "AcsCommunicationUserId" character varying(255) NULL;

    CREATE UNIQUE INDEX "UX_Users_AcsCommunicationUserId"
        ON "Users" ("AcsCommunicationUserId") WHERE "AcsCommunicationUserId" IS NOT NULL;

    COMMIT;
    ```
    Update `UserConfiguration.cs` to map the new nullable property (`HasMaxLength(255)`).

- [x] **Task 2 — Contracts (Shared)** (AC: all) — *fully implemented, including `AcsCallCredentialsDto`*
  - [x] `BoardCallDto` / `BoardCallParticipantDto`
  - [x] `BoardActionNotificationType.CallStarted/CallParticipantsChanged/CallEnded` (19–21)
  - [x] `CallStartedPayload` / `CallParticipantsChangedPayload` / `CallEndedPayload`
  - [x] **NEW** — `Contracts/DTOs/BoardCalls/AcsCallCredentialsDto.cs`: `public record AcsCallCredentialsDto(string Token, DateTimeOffset ExpiresOn, string AcsUserId, string RoomId);` — this is our own wrapper; **never expose an `Azure.Communication.*` SDK type through `Shared/Contracts`** (AD-3). `StartBoardCallCommand`/`JoinBoardCallCommand` return this alongside `BoardCallDto`.

- [x] **Task 3 — Backend: ACS infrastructure service** (AC: 1, 2, 3, 6)
  - [x] Added `Azure.Communication.Identity` 1.3.1 and `Azure.Communication.Rooms` 1.2.0 to `Infrastructure.csproj` only (both re-verified as current stable via the NuGet flat-container API at implementation time — no beta versions used). `Application.csproj` does not reference either package, per AD-1.
  - [x] `Application/Common/Enums/CallParticipantRole.cs` — `{ Attendee, Presenter }`, exactly as scoped.
  - [x] `Application/Interfaces/Services/IAcsCallService.cs` — implemented as scoped, with **one signature refinement**: `IssueTokenAsync` gained a `string roomId` parameter (`IssueTokenAsync(string acsUserId, string roomId, ...)`) so it can return a fully-formed `AcsCallCredentialsDto` (the original signature had no way to populate `AcsCallCredentialsDto.RoomId`).
  - [x] `Infrastructure/BoardCalls/AcsCallService.cs` — implemented against the real SDK surface (verified via Microsoft Learn API docs, not assumed): `CommunicationIdentityClient.CreateUserAsync()` / `.GetTokenAsync(CommunicationUserIdentifier, IEnumerable<CommunicationTokenScope>, ct)`; `RoomsClient.CreateRoomAsync(validFrom, validUntil, participants, ct)` / `.AddOrUpdateParticipantsAsync(roomId, participants, ct)` / `.RemoveParticipantsAsync(roomId, identifiers, ct)` / `.DeleteRoomAsync(roomId, ct)`. `RoomParticipant` takes a `CommunicationIdentifier` in its constructor and an settable `Role` (`ParticipantRole.Presenter`/`.Attendee` — both are static properties on an extensible `readonly struct`, not a plain enum). `EnsureUserIdentityAsync` is the only place a new ACS identity is ever created, persisted via `IUserRepository.Update` + `IUnitOfWork.SaveChangesAsync`.
  - [x] `Infrastructure/DI/Modules/AcsModule.cs` — registers `CommunicationIdentityClient`/`RoomsClient` as singletons (both constructed from the `AzureCommunicationServices` connection string via their `(string connectionString)` constructor overload, mirroring `BlobModule.cs`'s `BlobServiceClient` registration) and `IAcsCallService` as scoped; wired into `InfrastructureServiceCollectionExtensions.AddInfrastructure(...)`.
  - [x] Added `AzureCommunicationServices` connection string to `appsettings.json` (`ConnectionStrings`), `docker-compose.yml`'s `api` service environment block (`ConnectionStrings__AzureCommunicationServices=${AZURE_COMMUNICATION_SERVICES_CONNECTION}`), and `.env.example` (placeholder ACS connection-string format).

- [x] **Task 4 — Backend: CQRS lifecycle commands** (AC: 1, 2, 3, 6, 7)
  - [x] Extended `IBoardAccessService` with `EnsureCanStartCallAsync` (ScrumMaster/Admin, `requiresActiveBoard: true`) — mirrors `EnsureCanEditBoardAsync`'s exact one-liner delegation to `EnsureAccessAsync`, with a new `BoardRolePermissions.CanStartCall` predicate.
  - [x] `StartBoardCallCommand`: implemented as scoped — `EnsureCanStartCallAsync` → `ConflictException` if active call exists → `CreateRoomAsync` → create+save the `BoardCall` row → `EnsureUserIdentityAsync` → `AddOrUpdateParticipantAsync(..., Presenter)` → `IssueTokenAsync` → publish `CallStarted` (now carrying the full `BoardCallDto`, per the Task 3 review decision) → return `StartOrJoinBoardCallResponse`. Does not write a `BoardCallParticipant` row, per spec.
  - [x] `JoinBoardCallCommand`: implemented as scoped — `EnsureCanViewBoardAsync` → 404 if no active call → capacity check against `BoardCallConstants.MaxParticipants` → role mapped from `boardAccessContext.Role` (the already-resolved `BoardRole` from `EnsureCanViewBoardAsync`, no second lookup needed) → `EnsureUserIdentityAsync` → `AddOrUpdateParticipantAsync` → `IssueTokenAsync` → return `StartOrJoinBoardCallResponse`. Does not write a participant row.
  - [x] `LeaveBoardCallCommand`: implemented with one refinement — it does **not** call `EnsureUserIdentityAsync` (which would *provision* an ACS identity as a side effect of leaving). Instead it reads `User.AcsCommunicationUserId` directly via `IUserRepository` and no-ops if it's null (never had an identity, so never actually joined). Otherwise calls `RemoveParticipantAsync` as designed. **Known limitation, not fixed here:** since the Task 3 idempotent-404 patch is deferred, calling `Leave` for a participant already removed (or a call already ended) will currently propagate an unhandled `RequestFailedException` to the generic 500 handler rather than silently succeeding — Application can't safely catch that Azure SDK exception type itself without violating AD-1, so this must be fixed inside `AcsCallService` (the already-deferred patch), not worked around here.
  - [x] `EndBoardCallCommand`: implemented with one refinement for the same reason — it loads the active call via `GetActiveCallForBoardAsync` and throws `NotFoundException` if none exists, **before** calling `DeleteRoomAsync`. This means a repeat "end" call naturally 404s instead of ever invoking `DeleteRoomAsync` twice on an already-deleted room, sidestepping the not-yet-idempotent `DeleteRoomAsync` gap entirely for this call path (unlike `Leave`, which has no such natural guard available).
  - [x] `GetActiveBoardCallQuery`: implemented as scoped, unaffected by the pivot.
  - [x] `Application/Interfaces/Services/IBoardCallLifecycleService.cs` + `Application/Services/BoardCallLifecycleService.cs` — implemented in `Application/Services` (not Infrastructure), matching `BoardAccessService`'s existing precedent for an Application-layer service whose dependencies are all already Application-level abstractions (repositories, notifier, unit of work — no external SDK). `EndCallAsync` gained an optional `endedByUserId` parameter so the explicit "end for everyone" path attributes the `CallEnded` notification to the actual ScrumMaster/Admin who ended it, falling back to `call.StartedByUserId` for the auto-ended-because-empty path (Task 5).
  - [x] `BoardCallConstants.MaxParticipants = 4` — **added to the existing `Domain/Constants/BoardCallConstants.cs`** rather than creating a new, identically-named `Application/Common/Constants/BoardCallConstants.cs`. Two classes with the same name in different namespaces would have been exactly the kind of "didn't check for an existing convention" mistake Task 3's code review just caught for `CallParticipantRole` — consolidating avoids repeating it.
  - [x] `Application/Common/Mappings/BoardCallMappings.cs` — **new, not in the original Task 4 checklist**, added because `StartBoardCallCommand`/`JoinBoardCallCommand`/`GetActiveBoardCallQuery` all needed the same `BoardCall` → `BoardCallDto` projection. Matches the existing `BoardActionPositionMappings` precedent (`internal static class` under `Application/Common/Mappings/`) exactly.
  - [x] `Contracts/DTOs/BoardCalls/StartOrJoinBoardCallResponse.cs` — **new, not in the original Task 4 checklist** — `record StartOrJoinBoardCallResponse(BoardCallDto Call, AcsCallCredentialsDto Credentials)`, the actual return type `StartBoardCallCommand`/`JoinBoardCallCommand` needed (the story's Task 6 notes already assumed this type would exist by name; it just hadn't been created yet).
  - [x] ~~`GetIceServersConfigurationQuery`~~ — removed, as scoped.

- [x] **Task 5 — Backend: Event Grid webhook ingestion** (AC: 5, 6)
  - [x] Added `Azure.Messaging.EventGrid` 5.0.0 to `Presentation.csproj` (verified current stable via NuGet flat-container API), matching `project-context.md`'s guidance that a thin controller is fine here since parsing is only a few lines — no separate Application/Infrastructure-level handler was warranted for this.
  - [x] `Presentation/Controllers/BoardCallEventsController.cs` — thin controller. **One route deviation from the story's literal text**: `[Route("internal/board-call-events")]`, **not** `"api/internal/board-call-events"` as originally written. Reading `Program.cs`/`PrefixConventionConfigurator.cs` showed every `[ApiController]` already gets `"api"` auto-prepended, and `InternalBoardsController` is the real precedent (`[Route("internal/boards")]`, no `"api/"` prefix). Following the story's literal spec would have produced `/api/api/internal/board-call-events` — a path `InternalApiKeyMiddleware`'s `/api/internal` prefix check would **not** match, silently leaving the webhook unprotected by the `X-Internal-Api-Key` check. Caught before it shipped. Body implemented exactly as scoped:
    1. Deserializes the request body as `EventGridEvent[]` via `EventGridEvent.ParseMany(BinaryData)`.
    2. If the batch contains a `Microsoft.EventGrid.SubscriptionValidationEvent`, extracts `ValidationCode` and returns it — **one deviation**: no usable `SubscriptionValidationResponse` SDK type/constructor could be verified against live docs, so a local `internal sealed record SubscriptionValidationAck(string ValidationResponse)` matching the confirmed `{"validationResponse": "..."}` JSON contract is used instead, avoiding a guessed SDK API surface.
    3. For every other event, dispatches via `ISender` based on `EventType` — `CallParticipantAdded`/`CallParticipantRemoved` → `RecordCallParticipantJoinedCommand`/`RecordCallParticipantLeftCommand`; anything else logged at debug, not thrown. **Parameter naming deviation**: the command parameter is `AcsUserRawId`, not the story's `AcsParticipantRawId` — matches the confirmed ACS event JSON field name (`user.communicationIdentifier.rawId`) more directly.
  - [x] `Application/Features/BoardCalls/Commands/RecordCallParticipantJoinedCommand.cs` — looks up the `BoardCall` by `AcsRoomId` (no-op if none — stale/late event for an already-ended call), resolves the `User` by reverse lookup on `AcsCommunicationUserId` (no-op if none), creates or reactivates the `BoardCallParticipant` row, publishes `CallParticipantsChanged` with the full participant list (embedding the DTO, per the Task 1/2 review decision).
  - [x] `Application/Features/BoardCalls/Commands/RecordCallParticipantLeftCommand.cs` — same lookup, marks the matching active `BoardCallParticipant.LeftAt`, then calls `IBoardCallLifecycleService.EndCallIfEmptyAsync` in **both** the already-left no-op branch and the normal path (a redelivered "left" event could be the one that finally empties the call, so the check can't be skipped just because this delivery itself didn't change anything).
  - [x] Both commands are **idempotent** — check-then-reactivate-or-insert (Joined) / check-then-mark-or-noop (Left), safe under Event Grid's at-least-once delivery.
  - [x] **Necessary extensions beyond the original checklist**: `IBoardCallRepository`/`BoardCallRepository` gained `GetActiveCallByAcsRoomIdAsync`, `GetParticipantAsync` (any status, for reactivation), `AddParticipantAsync`, `UpdateParticipant`; `IUserRepository`/`UserRepository` gained `GetByAcsCommunicationUserIdAsync`. None of these existed after Task 1/4 because nothing needed a reverse ACS-identity-to-user lookup until this webhook did.

- [x] **Task 6 — Frontend: contracts, API client, store** (AC: all)
  - [x] `Services/Api/IBoardCallsApi.cs` (Refit) — `StartAsync`, `JoinAsync`, `LeaveAsync`, `EndAsync`, `GetActiveAsync`, mirroring `ITasksApi`'s shape. `Start`/`Join` return `IApiResponse<StartOrJoinBoardCallResponse>` where `StartOrJoinBoardCallResponse` wraps `(BoardCallDto Call, AcsCallCredentialsDto Credentials)` — no `GetIceServersAsync` (removed with Task 4's query). `GetActiveAsync` returns `IApiResponse<BoardCallDto?>` — nullable, since "no active call" is a legitimate response, not an error.
  - [x] `Services/BoardCalls/BoardCallApiService.cs` + `IBoardCallApiService` — mirrors `TaskApiService`'s `HandleResponseAsync()` pattern, with **one necessary refinement**: `GetActiveCallAsync` cannot use the generic `HandleResponseAsync<T>()` extension as-is, because that extension treats a `null` `Content` on a successful response as a failure (throws) — which would misfire on the ordinary "no active call" case. Fixed by casting to the non-generic `IApiResponse` for the success/error check (reusing the exact same error-extraction logic) and returning `response.Content` (possibly null) directly, rather than duplicating or modifying the shared extension.
  - [x] `Services.Abstractions/BoardCalls/IBoardCallStore.cs` + `Services/BoardCalls/Stores/BoardCallStore.cs` — holds `BoardCallDto? ActiveCall`, `ApplyAction(BoardActionNotification, Guid)` switching on `CallStarted`/`CallParticipantsChanged`/`CallEnded`, exactly as scoped. **Necessary additions beyond the literal checklist bullet** (required by `project-context.md`'s fixed frontend call chain — components must inject the Store only, never the `*ApiService`/Refit interface directly, so the Store needs more than just `ActiveCall`/`ApplyAction` to be usable by Task 8's UI): `BoardId`, `IsLoading`, `ErrorMessage`, `StateChanged` (mirroring `BoardDetailsStore`'s shape), plus `LoadActiveCallAsync`/`StartCallAsync`/`JoinCallAsync`/`LeaveCallAsync`/`EndCallAsync` wrapping the API service. `StartCallAsync`/`JoinCallAsync`/`EndCallAsync` optimistically update `ActiveCall` right after their API call succeeds (mirroring `BoardDetailsStore`'s optimistic-update convention) — necessary because `BoardActionSyncGuard.TryAccept` deliberately rejects a notification whose `ActorUserId` matches the current user, so the actor's own client would never see their own action reflected via the real-time notification path alone. `LeaveCallAsync` deliberately does **not** touch `ActiveCall` locally — the leaving user no longer needs the participant list once they've left; this is left to Task 8's UI to react to (e.g. by leaving the call panel), keeping Task 6 to a store facade only.
  - [x] `BoardActionSyncKey.cs` — added proper per-entity keys for the 3 call notification types (`$"call:{BoardCallId}"`), closing the gap explicitly deferred here from the Task 1/2 review (previously fell through to the generic `type:{byte}` fallback).
  - [x] Wired into `BoardActionsHubService.ConnectAsync`'s existing dispatch (third `.ApplyAction(...)` call), unchanged in shape from the original design.
  - [x] Registered everything in `Services/DI/ServiceCollectionExtensions.cs` per the existing paired-registration pattern.
  - [x] **Necessary prerequisite not itemized anywhere in this story (Task 4 nor Task 6)**: `Presentation/Controllers/BoardCallsController.cs` did not exist — Task 4 built the CQRS commands/query but never wired any HTTP endpoint to dispatch them, so there was nothing for `IBoardCallsApi` to call. Created it mirroring `TasksController`'s exact shape (`[Route("boards/{boardId:guid}/calls")]`, `[Authorize]`, thin actions dispatching via `ISender`): `GET active`, `POST start`, `POST join`, `POST leave`, `POST end`.

- [x] **Task 7 — Frontend: esbuild bundling + ACS Calling SDK JS interop** (AC: 2, 3, 4)
  - [x] Added `src/Frontend/WebApp/package.json` — this repo's first build-tooling dependency. Dependencies: `@azure/communication-calling` `^1.43.1`, `esbuild` `^0.28.1` (devDependency) — both re-verified as current stable directly against the npm registry before pinning. **One necessary addition beyond the story's literal dependency list**: `@azure/communication-common` `^2.4.2` — required for `AzureCommunicationTokenCredential`, which `createCallAgent` needs; the story's checklist named only `@azure/communication-calling`. `npm install` was run locally to generate a real `package-lock.json` (committed, required for `npm ci` in the Dockerfile to work at all), and `npm run build` was verified to actually produce `wwwroot/js/acsCallInterop.bundle.js` (7.3 MB, expected for the full bundled Calling SDK).
  - [x] `src/Frontend/WebApp/js-src/acsCallInterop.js` — the bundler entry point (lives outside `wwwroot`; only the bundled output is served). All 6 functions implemented exactly as scoped, verified against the live ACS Calling SDK reference (Microsoft Learn's `Call` interface page + the official Rooms quickstart sample) rather than assumed:
    - `initCallAgent(userToken, displayName)` → `new CallClient()` → `callClient.createCallAgent(new AzureCommunicationTokenCredential(userToken), { displayName })`, also caches `callClient.getDeviceManager()`.
    - `joinRoom(roomId, dotNetObjectRef)` → `deviceManager.askDevicePermission({ audio: true, video: true })` first (confirmed via live docs: this resolves to `{ audio, video }` booleans, never throws on denial — the mechanism behind AC4's audio-only/view-only join), only attaches a `LocalVideoStream` if video access was granted **and** a camera exists, then `callAgent.join({ roomId }, { videoOptions })`. Wires `call.on('remoteParticipantsUpdated', ...)` (plus per-participant `stateChanged`/`videoStreamsUpdated`) to call back into a cached `DotNetObjectReference` via `invokeMethodAsync("OnRemoteParticipantsChanged", ...)`. Returns `{ audioAvailable, videoAvailable }` so the C# side knows what was actually granted.
    - `toggleMic(enabled)` → `call.mute()`/`call.unmute()`; `toggleCamera(enabled)` → `call.startVideo(localVideoStream)`/`call.stopVideo(localVideoStream)`, lazily creating the `LocalVideoStream` if the user turns video on after initially joining audio-only.
    - `startScreenShare()` / `stopScreenShare()` — **corrected from the story's assumed method names.** The story's checklist named these `startScreenSharingAsync()`/`stopScreenSharingAsync()`; the actual, current GA Web SDK method names (confirmed directly against Microsoft Learn's `Call` interface reference) are `call.startScreenSharing()` / `call.stopScreenSharing()` — no `Async` suffix, matching general JS SDK naming conventions rather than the C#-style naming the story assumed. Used the verified names.
    - `attachRenderer(streamId, videoElementId)` — generic over a `streamId` that's either the sentinel `"local"` or a remote participant's `communicationUserId` (confirmed as the stable identifier shape on `remoteParticipant.identifier`), so one function renders any tile (local preview or any remote participant) via `VideoStreamRenderer`. Disposes any prior renderer for the same `streamId` before creating a new one.
    - `leaveCall()` → `call.hangUp()`, then disposes every active renderer and resets module state.
  - [x] `Services/BoardCalls/AcsCallInteropService.cs` (+ `Services.Abstractions/BoardCalls/IAcsCallInteropService.cs`, following this codebase's universal interface-per-service convention) — owns the local call session, caches the imported JS module (`IJSObjectReference`, imported once via `IJSRuntime.InvokeAsync<IJSObjectReference>("import", ...)` and reused — never re-imported per action, per the story's explicit caveat), exposes a `RemoteParticipantsChanged` C# event fed by a `[JSInvokable]` callback method. **One deliberate deviation from the story's literal text**: orchestrates `IBoardCallStore` (Task 6), not `IBoardCallApiService` directly as the checklist said. Calling the Refit-backed API service directly here, bypassing the Store, would mean the actual Start/Join HTTP call happens through a *second*, separate path from whatever the future UI (Task 8) calls on the Store to keep `ActiveCall` in sync — duplicating the real network call and its side effects (occupying a capacity seat, issuing a second ACS token) rather than sharing one. Orchestrating through the Store means exactly one Start/Join call happens, and `ActiveCall` state and the live ACS session are always driven from the same response.
  - [x] Updated `src/Frontend/WebApp/Dockerfile` to install Node.js and run the bundler before `dotnet publish`, matching the story's snippet (also copies `package-lock.json`, required by `npm ci`). **Verified for real** — ran `docker build` against the updated Dockerfile end-to-end (Node install → `npm ci` → `npm run build` → `dotnet publish` → nginx image), not just written and assumed correct.
  - [x] Documented the local (non-Docker) dev step in the root `README.md` (no separate frontend README exists in this repo) — `npm install && npm run build` must run at least once, and again after any `js-src/` change, before `dotnet run`.
  - [x] Added `src/Frontend/WebApp/wwwroot/js/*.bundle.js` to `.gitignore` (`node_modules/` was already covered by an existing generic pattern) — resolves the "confirm this convention when Task 7 is implemented" note from the Task 1/2 review's Project Structure Notes: the bundle is a generated build artifact, consistent with the `npm run build`-before-`dotnet run` step this task documents, so it should not be checked in.

- [x] **Task 8 — Frontend: UI components** (AC: 1, 2, 3, 4, 6)
  - [x] `BoardCallIndicator.razor` — shows the live "Call in progress · N/4 joined" status from `IBoardCallStore.ActiveCall` and a Start/Join button; mirrors `BoardExportControls.razor`'s live-status button *pattern* (state-driven conditional buttons, `StateChanged` subscribe/unsubscribe in `OnInitialized`/`Dispose`) without repeating its AD-9 Store-bypass violation (`BoardExportControls` calls `IBoardApiService` directly — this component never does). Start/Join buttons call `IAcsCallInteropService.StartCallAsync`/`JoinCallAsync` (not `IBoardCallStore` directly), per the correction already noted above. Injects `IBoardDetailsStore` for the role check (`CanStartCall` = Admin/ScrumMaster). `MaxParticipants = 4` is a local constant mirroring the backend's `BoardCallConstants.MaxParticipants` — **known, accepted duplication**: no DTO currently exposes this value to the frontend, and adding one was out of this task's scope; flagged in Completion Notes.
  - [x] `BoardCallPanel.razor` — in-call surface: local video/audio controls, one tile per remote participant (rendered from `IAcsCallInteropService.RemoteParticipantsChanged`, using each `RemoteParticipantInfo.Id` as the `streamId` passed to `AttachRendererAsync`), mic/camera/screen-share toggle buttons, "Leave" button, (ScrumMaster/Admin only) "End call for everyone" button — all via `IAcsCallInteropService`, never duplicating calls directly on `IBoardCallStore`. Renders only while `IAcsCallInteropService.IsInCall`; attaches video renderers from `OnAfterRenderAsync` (container-exists-first ordering). Participant tile labels use `RemoteParticipantInfo.DisplayName` directly rather than cross-referencing `IBoardCallStore.ActiveCall.Participants` — **necessary correction to this task's own earlier-written checklist text** (which had said to use the Store for "`DisplayName`/`AvatarUrl`"): `RemoteParticipantInfo.Id` is the ACS-side `communicationUserId`, while `BoardCallParticipantDto.UserId` is our internal `Guid` — there is no shared key to cross-reference them on the frontend, so the Store's participant list isn't actually usable here. Not a real gap: `RemoteParticipantInfo.DisplayName` already carries a usable name, since every participant's own client sets it via their own `profileStore.Profile?.DisplayName` when creating their call agent.
  - [x] **Necessary addition beyond the literal Task 7/8 checklist text, discovered while wiring these two components together**: `IAcsCallInteropService` had no general `StateChanged` event — only `RemoteParticipantsChanged`. Without one, nothing notified `BoardCallPanel` to start rendering the moment `BoardCallIndicator`'s Start/Join button succeeded (both components share the same scoped instance in Blazor WASM's single-scope DI model, but had no way to know about each other's triggered state transitions). Added `event Action? StateChanged;`, raised after a successful join (`JoinAcsRoomAsync`) and after any local session teardown (`EndLocalSessionAsync`).
  - [x] **Second necessary addition, also required for AC6 to actually hold on the client that's still connected**: nothing detected the ACS call disconnecting from the *other* side — e.g. a ScrumMaster/Admin clicking "End call for everyone" revokes Room access and ACS itself disconnects everyone still in the room, but `AcsCallInteropService.IsInCall` had no way to learn that and would report stale "still in call" state, leaving `BoardCallPanel` visibly rendered with a dead session. Added a `call.on("stateChanged", ...)` listener in `acsCallInterop.js` that invokes a new `[JSInvokable] OnCallDisconnected()` when `call.state === "Disconnected"`; it resets local flags and raises `StateChanged` without re-calling the backend Leave/EndCallAsync (the backend record is already correct by the time this fires — that call already happened, made by whoever actually ended it).
  - [x] Wired into `Pages/Boards/Board.razor`: `IBoardCallStore.Reset()`/`LoadActiveCallAsync(BoardId)` alongside the existing `BoardDetailsStore` reset/load (gated by `IsArchived: false`, matching `ActionsHubService.ConnectAsync`'s existing gating); `<BoardCallIndicator/>` rendered next to `<BoardExportControls/>` in the header; `<BoardCallPanel/>` rendered prominently below the header when the board is loaded and not archived. **Necessary addition**: `Board.razor`'s `Dispose()` now calls `CallInterop.LeaveCallAsync()` (fire-and-forget, matching the existing `ExportHubService.DisconnectAsync()` pattern) if still in a call when navigating away — `IAcsCallInteropService` is scoped app-lifetime in Blazor WASM, so nothing else would ever end a session left running in the background after the user leaves the board page.

### Review Findings

_Code review of Task 1 & Task 2 implementation, 2026-07-25. Blind Hunter + Edge Case Hunter + Acceptance Auditor (vs. this spec + `project-context.md` + `ARCHITECTURE-SPINE.md`)._

- [x] [Review][Decision] `DeleteBehavior.Restrict` vs `Cascade` for `BoardCall`→`Board` and `BoardCallParticipant`→`BoardCall` — **Resolved: match sibling convention, `Cascade`.** `ColumnConfiguration`/`TaskItemConfiguration`'s `Cascade` pattern now applies here too; both FKs (EF config + `0020` SQL) changed from `Restrict` to `Cascade`. (The `→User` FKs on both entities, e.g. `StartedByUserId`, remain `Restrict` — unaffected by this decision.)
- [x] [Review][Decision] Thin real-time payloads (`CallStartedPayload`, `CallParticipantsChangedPayload`) — **Resolved: embed the full DTO**, matching `TaskCreatedPayload`/`CommentAddedPayload`. Avoids the N+1 where a client receives an ID and must immediately fire a follow-up request just to render `DisplayName`/`AvatarUrl`. `CallStartedPayload` now carries `BoardCallDto Call`; `CallParticipantsChangedPayload` now carries `IReadOnlyList<BoardCallParticipantDto> Participants` instead of bare `Guid`s. `CallEndedPayload` stays ID-only — there's nothing to render for an ended call, matching the sibling "deleted" payload shape where no richer data applies.

- [x] [Review][Patch] Hardcoded max-length literals instead of `Domain.Constants` — **Fixed.** Added `Domain/Constants/BoardCallConstants.cs` (`MaxAcsRoomIdLength = 128`, `MaxAcsCommunicationUserIdLength = 255`), referenced from both configs, matching `SubscriptionConstants`/`ColumnConstants`/`TaskItemConstants`. [`src/Backend/Persistence/Configurations/BoardCallConfiguration.cs`, `src/Backend/Persistence/Configurations/UserConfiguration.cs`]
- [x] [Review][Patch] Unique-index naming convention broken — **Fixed.** All 4 new indexes renamed `UX_...` → `IX_...` in `0020`/`0021`, matching every prior migration's exceptionless convention (unique or not, indexes are named `IX_`). [`src/Backend/Database/Scripts/0020_CreateBoardCallTables.sql`, `src/Backend/Database/Scripts/0021_AddAcsCommunicationUserIdToUsers.sql`]
- [x] [Review][Patch] Missing invariant `CHECK` constraints — **Fixed.** Added `CK_BoardCalls_EndedAfterStarted`, `CK_BoardCalls_AcsRoomId_NotEmpty`, `CK_BoardCallParticipants_LeftAfterJoined`, `CK_Users_AcsCommunicationUserId_NotEmpty` — inline in `0020`'s `CREATE TABLE`s (never applied, safe to amend) and via `ALTER TABLE ... ADD CONSTRAINT` in `0021` (matches `0015_AddArchivationFields.sql`'s pattern for an existing table), mirrored in each EF config via `HasCheckConstraint` (matching `ColumnConfiguration`/`TaskItemConfiguration`). [`src/Backend/Database/Scripts/0020_CreateBoardCallTables.sql`, `src/Backend/Database/Scripts/0021_AddAcsCommunicationUserIdToUsers.sql`, `src/Backend/Persistence/Configurations/BoardCallConfiguration.cs`, `BoardCallParticipantConfiguration.cs`, `UserConfiguration.cs`]
- [x] [Review][Patch] `BoardRepository.SoftDeleteCascadeAsync` not extended for the new tables — **Fixed.** Added `BoardCallParticipants` (joined via `p.BoardCall!.BoardId`) and `BoardCalls` cascade soft-delete steps, inserted after `BoardMembers` and before the final `Boards` update — same `ExecuteUpdateAsync` pattern as every sibling child table. [`src/Backend/Persistence/Repositories/BoardRepository.cs`]

- [x] [Review][Defer] Global per-user single-active-call-across-boards constraint isn't enforced (a user could be "active" in calls on two different boards at once) — deferred, speculative and not covered by any stated AC.
- [x] [Review][Defer] No compensating cleanup for a partial ACS-Room-created/DB-insert-failed (or vice versa) failure in the start/join flow — deferred, this is Task 3/4 (command handler) territory; nothing to implement yet at the Task 1/2 (schema-only) layer.
- [x] [Review][Defer] `src/Frontend/Services/Boards/BoardActionSyncKey.cs` doesn't map the 3 new `BoardActionNotificationType` values (falls through to the generic default case, which is not fatal) — deferred to Task 6, where the frontend Store consuming these types is actually built.
- [x] [Review][Defer] `User.AcsCommunicationUserId` has no issuance/rotation/revocation metadata, so a row can silently point at an ACS identity deleted independently on Azure's side — deferred, schema-only hardening for a later pass once Task 3's `IAcsCallService.EnsureUserIdentityAsync` exists to actually own this lifecycle.

### Findings dismissed as noise or already-decided
- Stale-active-row/heartbeat/TTL concern for crashed clients — superseded by design: the story's Task 5 (Event Grid `CallParticipantRemoved` webhook) replaces the need for this entirely; not a gap in Task 1/2.
- No workspace-entitlement/plan-gating wiring — already an explicit, documented product decision (Dev Notes §5/§7: calls are ungated for now).
- `CountActiveParticipantsAsync` has no caller yet — intentional scaffolding for Task 4's capacity check, working as designed.
- Migration comment citing "AC #1" — traceable to this story file's own AC numbering, not a real issue.
- SQL comment density in `0020` (Acceptance Auditor, informational only) — auditor itself concluded this is not a spec violation.
- Domain entity setters don't self-enforce `EndedAt >= StartedAt` invariants in code — inconsistent with this codebase's established anemic-entity convention (no sibling entity does this); the new DB `CHECK` constraint (patch above) is the actual authority per AD-5.
- Null-guard on `CallParticipantsChangedPayload.ParticipantUserIds` — inconsistent with the guard-free convention used by all 15+ sibling payload records.

### Review Findings — Task 3

_Code review of Task 3 (ACS infrastructure service) implementation, 2026-07-25. Blind Hunter + Edge Case Hunter + Acceptance Auditor (vs. this spec + `project-context.md` + `ARCHITECTURE-SPINE.md`)._

- [x] [Review][Patch] `EnsureUserIdentityAsync` commits the Unit of Work itself — **Fixed.** Removed the inline `SaveChangesAsync`/`IUnitOfWork` dependency from `AcsCallService` entirely; it now only mutates the tracked `User` entity. `StartBoardCallCommand` reordered to resolve the ACS identity *before* its existing `SaveChangesAsync` (so the identity change and the new `BoardCall` row commit together); `JoinBoardCallCommand` gained its own explicit `SaveChangesAsync` call after `EnsureUserIdentityAsync`, since it previously had nothing to commit. [`src/Backend/Infrastructure/Boards/Calls/AcsCallService.cs`, `src/Backend/Application/Features/BoardCalls/Commands/StartBoardCallCommand.cs`, `JoinBoardCallCommand.cs`]
- [x] [Review][Patch] `CallParticipantRole` placed in a brand-new `Application/Common/Enums/` folder instead of the established sibling convention — **Fixed.** Moved to `Domain/Enums/CallParticipantRole.cs` (`: byte`, explicit values, matching `BoardRole`/`WorkspaceRole`'s exact style). All referencing files updated. [`src/Backend/Domain/Enums/CallParticipantRole.cs`]
- [x] [Review][Patch] New top-level `Infrastructure/BoardCalls/` folder duplicates rather than extends the existing convention — **Fixed.** Moved to `Infrastructure/Boards/Calls/AcsCallService.cs`, namespace `Infrastructure.Boards.Calls`; `AcsModule.cs`'s `using` updated. [`src/Backend/Infrastructure/Boards/Calls/AcsCallService.cs`]
- [x] [Review][Patch] No `RequestFailedException` translation anywhere in `AcsCallService` — **Fixed.** `AddOrUpdateParticipantAsync`/`IssueTokenAsync` catch a 404 and throw `NotFoundException`; `RemoveParticipantAsync`/`DeleteRoomAsync` catch a 404 and treat it as an idempotent no-op (this was the one patch causing an actual reachable bug — `LeaveBoardCallCommand` calling `RemoveParticipantAsync` on an already-left participant). `CreateRoomAsync` logs and rethrows on any failure (no 404 semantic applies to a creation call). [`src/Backend/Infrastructure/Boards/Calls/AcsCallService.cs`]
- [x] [Review][Patch] No input validation on any public method — **Fixed.** `ArgumentException.ThrowIfNullOrWhiteSpace` on every `roomId`/`acsUserId` parameter, `ArgumentOutOfRangeException.ThrowIfEqual(userId, Guid.Empty)` on `EnsureUserIdentityAsync`, matching `StripeSubscriptionsService`'s convention. [`src/Backend/Infrastructure/Boards/Calls/AcsCallService.cs`]
- [x] [Review][Patch] `AcsModule`'s connection-string check (`?? throw`) doesn't catch an empty/whitespace value — **Fixed.** Changed to an explicit `string.IsNullOrWhiteSpace` check. [`src/Backend/Infrastructure/DI/Modules/AcsModule.cs`]
- [x] [Review][Patch] No logging anywhere in `AcsCallService` — **Fixed.** Added `ILogger<AcsCallService>`; every method logs on success (`LogInformation`) and the 404/idempotent-no-op paths log at `LogWarning`/`LogDebug` as appropriate. [`src/Backend/Infrastructure/Boards/Calls/AcsCallService.cs`]
- [ ] [Review][Patch] `.env.example` still has no trailing newline after this diff added a second line — violates `.editorconfig`'s top-level `insert_final_newline = true`, and this diff touched the file anyway. **Explicitly left unapplied — user chose not to select this one when the other 7 were approved.** [`.env.example`]

- [x] [Review][Defer] Race condition in `EnsureUserIdentityAsync`'s check-then-act (two concurrent requests for the same user both see no `AcsCommunicationUserId` and both call `CreateUserAsync`) — real but narrow (double-click/multi-tab); the DB unique index (`IX_Users_AcsCommunicationUserId`) at least prevents silent duplicate persistence. Proper handling (catch-and-retry on unique violation) needs Task 4's actual command handler to exist.
- [x] [Review][Defer] Broadened the existing `deferred-work.md` item on ACS/DB partial-failure cleanup to explicitly also cover `EnsureUserIdentityAsync` (an ACS identity created but the surrounding save fails) — same class of problem as the already-deferred Room-creation case, not just Room-specific.
- [x] [Review][Defer] `CreateRoomAsync` always passes `validFrom: null, validUntil: null`, deferring to ACS's default room TTL with no reconciliation against `BoardCall.EndedAt`'s lifecycle — Task 4's call once an actual call-duration business rule exists.
- [x] [Review][Defer] No ACS emulator exists (unlike Azurite for Blob Storage) — local dev requires a real (or shared dev) ACS resource to boot the API at all once the connection-string check is strict. Genuine DX gap, folded into the existing dev-tunnel note in Dev Notes.

### Findings dismissed as noise, already-decided, or a misread of the ACS security model
- `IssueTokenAsync` not verifying room membership before issuing a token — misreads the ACS model: the token is a general VoIP-scope credential, not room-scoped; Room membership is enforced by ACS itself at join time, not by token issuance.
- "`AcsModule` should use an `Options` class instead of inline `?? throw`" — `DatabaseModule.cs` already establishes an equally valid inline-connection-string-check precedent for exactly this kind of single-value config; `Options` classes are for multi-field structured config (`BoardExportSchedulerOptions`), not warranted here.
- "`AcsModule`'s `InvalidOperationException` should be an `AppException`" — wrong exception category; this fires at DI-composition/startup time, before any HTTP request exists to render a `ProblemDetails` response for, and matches `DatabaseModule.cs`'s identical pattern.
- `IAcsCallService` has zero consumers in this diff — working as designed; Task 4 is where it gets called.
- Package version pinning "has no stated rationale" — both versions were verified directly against the live NuGet API before being pinned (see Task 3's Completion Notes); just not visible from the diff alone.
- `ToAcsRole`'s `ArgumentOutOfRangeException` default arm called "dead code" — standard idiomatic exhaustive-switch defensive pattern, not a real gap.
- Pre-existing `Application.csproj` → `Azure.Storage.Blobs` AD-1 tension — explicitly not introduced by this diff, out of scope.

### Review Findings — Task 4 & Task 5

_Code review of Task 4 (CQRS lifecycle commands) and Task 5 (Event Grid webhook ingestion), 2026-07-25. Blind Hunter + Edge Case Hunter + Acceptance Auditor (vs. this spec + `project-context.md`), all three converged independently on the two `[high]` findings below._

- [x] [Review][Decision] Participant-capacity enforcement (`MaxParticipants = 4`) is unreliable by construction — `JoinBoardCallCommand.CountActiveParticipantsAsync` only counts `BoardCallParticipant` rows, and per this story's own design those rows are written **only** by the async `RecordCallParticipantJoinedCommand` Event Grid webhook (Start/Join deliberately "do not write a participant row"). Two consequences confirmed by reading `JoinBoardCallCommand.cs:27-36`: (1) concurrent or closely-timed joins can all read the same stale (too-low) count and all pass the `< 4` check, oversubscribing the ACS room past the stated cap — not a narrow race, reachable any time two people join within the webhook's normal round-trip latency; (2) a participant reconnecting (page refresh) to an already-full call is wrongly rejected, since their own existing membership isn't excluded from the count. **Resolved: option (a).** `StartBoardCallCommand`/`JoinBoardCallCommand` now insert the `BoardCallParticipant` row synchronously as a reservation, in the same transaction as the DB writes that made the count trustworthy at check time; `JoinBoardCallCommand` also now checks for the caller's own existing active row first and skips both the reservation and the capacity check for a reconnect. If the subsequent ACS calls (`AddOrUpdateParticipantAsync`/`IssueTokenAsync`) fail after the reservation is committed, both handlers roll it back (delete the participant row, and for Start also the `BoardCall` row + ACS room) before rethrowing, so a failed start/join never leaves a phantom seat. This also required updating `RecordCallParticipantJoinedCommand` (see note below) — the webhook's own "create or reactivate" logic could otherwise race against the new synchronous reservation and violate the partial unique index. No new Hangfire/background cleanup job was introduced — synchronous rollback fully closes the gap without reintroducing the grace-period mechanism Dev Notes §3 explicitly removed. [`src/Backend/Application/Features/BoardCalls/Commands/JoinBoardCallCommand.cs`, `StartBoardCallCommand.cs`, `RecordCallParticipantJoinedCommand.cs`]

- [x] [Review][Patch] `BoardCallLifecycleService.EndCallAsync` never calls `IAcsCallService.DeleteRoomAsync` — **Fixed.** `EndCallAsync` now injects `IAcsCallService` and calls `DeleteRoomAsync` after committing `EndedAt`, covering both the auto-end (`EndCallIfEmptyAsync`) and explicit-end paths from one place; `EndBoardCallCommand`'s own now-redundant `DeleteRoomAsync` call was removed. [`src/Backend/Application/Services/BoardCallLifecycleService.cs`, `src/Backend/Application/Features/BoardCalls/Commands/EndBoardCallCommand.cs`]
- [x] [Review][Patch] Explicit "end call for everyone" permanently orphans other participants' `BoardCallParticipant` rows — **Fixed.** `EndCallAsync` now loads the call via `GetActiveCallWithParticipantsAsync` and synchronously sets `LeftAt` on every still-open participant row in the same `SaveChangesAsync` as `EndedAt`, before deleting the room — so it no longer depends on each participant's own `CallParticipantRemoved` webhook (which would otherwise arrive after `EndedAt` is already set and no-op). [`src/Backend/Application/Services/BoardCallLifecycleService.cs`]
- [x] [Review][Patch] `StartBoardCallCommand` doesn't translate a concurrent double-start's unique-constraint violation into `ConflictException` — **Fixed, at the root.** `UnitOfWork.SaveChangesAsync` now catches a `DbUpdateException` wrapping a Postgres `23505` unique-violation (`PostgresErrorCodes.UniqueViolation`) and throws `Domain.Exceptions.ConflictException` — a general safety net for any genuine unique-constraint race across the app, not just this one, since Application cannot reference `Npgsql`/EF types directly per AD-1. [`src/Backend/Persistence/Repositories/UnitOfWork.cs`]
- [x] [Review][Patch] `StartBoardCallCommand` creates the ACS room before persisting the `BoardCall` row, with no compensating cleanup if `SaveChangesAsync` fails afterward — **Fixed.** The room-creation-to-DB-commit section is now wrapped in a try/catch that calls `DeleteRoomAsync` on any failure (including the new translated `ConflictException`) before rethrowing. [`src/Backend/Application/Features/BoardCalls/Commands/StartBoardCallCommand.cs`]
- [x] [Review][Patch] `BoardCallEventsController` silently drops malformed `CallParticipantAdded`/`CallParticipantRemoved` events with no log entry — **Fixed.** Added an `else` branch logging at `LogWarning` when `Room.Id`/`User.CommunicationIdentifier.RawId` is missing. [`src/Backend/Presentation/Controllers/BoardCallEventsController.cs`]
- [x] [Review][Patch] `EventGridEvent.ParseMany` isn't wrapped in a try/catch — **Fixed.** Wrapped in a try/catch for `JsonException` (verified as the type this SDK method throws on malformed JSON), returning `BadRequest()` and logging a warning instead of an unhandled 500. [`src/Backend/Presentation/Controllers/BoardCallEventsController.cs`]
- [x] [Review][Patch] Cosmetic: stray double blank line in `BoardAccessService.cs` — **Fixed.** [`src/Backend/Application/Services/BoardAccessService.cs`]
- [x] [Review][Patch] Cosmetic/naming: `EndBoardCallCommand` authorizes via `EnsureCanStartCallAsync` — **Fixed.** Renamed the underlying predicate `BoardRolePermissions.CanStartCall` → `CanManageCall` (matching the `CanManage*` convention already used by `CanManageColumns`/`CanManageTasks`/etc.), and added a distinct `IBoardAccessService.EnsureCanEndCallAsync` (same predicate, its own error message) for `EndBoardCallCommand` to call instead of reusing `EnsureCanStartCallAsync`. [`src/Backend/Domain/Authorization/BoardRolePermissions.cs`, `src/Backend/Application/Interfaces/Services/IBoardAccessService.cs`, `src/Backend/Application/Services/BoardAccessService.cs`, `src/Backend/Application/Features/BoardCalls/Commands/EndBoardCallCommand.cs`]

- **Necessary follow-on fix, not one of the original 8/9 findings above, caught while implementing the capacity-race decision**: `RecordCallParticipantJoinedCommand` previously used `GetParticipantAsync` (any status) and would "reactivate" a stale (already-left) row if one existed. With `Join` now creating a fresh row per session rather than reusing an old one, a rejoin can leave multiple historical rows for the same `(BoardCallId, UserId)` — reactivating the wrong one would set two rows' `LeftAt` to `null` simultaneously, violating the partial unique index and turning into a `ConflictException` that Event Grid would retry forever against the same conflict. Fixed by switching the lookup to `GetActiveParticipantAsync` (the single row that's allowed to have `LeftAt IS NULL`): if found, no-op (the synchronous reservation already recorded it); if not found (defense-in-depth only — shouldn't happen under the normal Start/Join flow), create a fresh row instead of guessing which stale one to reactivate. The now-unused `GetParticipantAsync` repository method was removed. [`src/Backend/Application/Features/BoardCalls/Commands/RecordCallParticipantJoinedCommand.cs`, `src/Backend/Application/Interfaces/Repositories/IBoardCallRepository.cs`, `src/Backend/Persistence/Repositories/BoardCallRepository.cs`]

- [x] [Review][Defer] Out-of-order Event Grid delivery isn't handled — a stale `CallParticipantAdded` arriving after a newer `CallParticipantRemoved` for the same participant would incorrectly reopen them as active. Real but narrow (Event Grid delivers near-order in practice; only retries would reorder), and no stated AC requires strict ordering. Fixing it needs per-participant sequencing state not currently modeled — deferred rather than blocking this story.
- [x] [Review][Defer] `BoardCallEventsController` relies on the same shared static `X-Internal-Api-Key` already used by every other `/api/internal/*` endpoint (`InternalApiKeyMiddleware`) — a leaked key lets anyone forge participant join/leave events. Pre-existing architecture from before this story; not something this diff can unilaterally redesign.
- [x] [Review][Defer] `RecordCallParticipantJoinedCommand`/`RecordCallParticipantLeftCommand`: if `GetActiveCallWithParticipantsAsync` returns `null` because the call ended concurrently, the fallback `?? call` sends a `CallParticipantsChanged` notification with an empty participant list. Self-corrects on the next fetch; narrow race, low impact, not worth the complexity of a proper fix for this story's scope.

### Findings dismissed as noise, already-decided, or a misread of the async design
- No `IRequireWorkspaceFeature` gating on any call command — already an explicit, documented product decision (Dev Notes §5/§7: calls are ungated for now).
- Rejoin logic reuses the same `BoardCallParticipant` row instead of a new row per join/leave session — per this story's own spec text ("mirrors the idempotent-rejoin logic... that logic moves here"); no stated AC needs per-session join history.
- `BoardCallEventsController` missing an explicit `[AllowAnonymous]`/`[Authorize]` attribute — checked against `InternalBoardsController`, the actual sibling convention for `/api/internal` controllers, which also has neither attribute; consistent, not a deviation.
- A single Event Grid delivery batch mixing a `SubscriptionValidationEvent` with real participant events would drop the real ones — not reachable in practice: Azure's subscription-validation handshake fires once, standalone, at subscription-creation time, never bundled with live event batches afterward.
- `sender.Send` throwing mid-batch aborts the whole delivery and triggers an Event Grid retry of the entire batch — this is the correct, intended error-handling pattern given every handler here is already idempotent; not a bug.
- `JoinBoardCallCommand`/`LeaveBoardCallCommand` racing against a concurrent `EndBoardCallCommand` (room deleted mid-flight) — already degrades gracefully via Task 3's exception-translation patch (`AddOrUpdateParticipantAsync`/`IssueTokenAsync` → clean `NotFoundException`; `RemoveParticipantAsync` → idempotent no-op).
- `EndBoardCallCommand` throwing `NotFoundException` if the call already auto-ended between the admin's click and the lookup — reasonable behavior (nothing left to end), not a bug.

### Review Findings — Task 6 & Task 7 (frontend)

_Code review of all frontend changes so far (Task 6 + Task 7), 2026-07-25. Blind Hunter + Edge Case Hunter + Acceptance Auditor (vs. this spec + `project-context.md`), scoped to `src/Frontend/`, `README.md`, `.gitignore` only — backend already reviewed separately._

- [x] [Review][Patch] `AcsCallInteropService.StartCallAsync`/`JoinCallAsync` have no compensating cleanup if `JoinAcsRoomAsync` fails after `boardCallStore.StartCallAsync`/`JoinCallAsync` has already committed the reservation server-side — confirmed by reading the file: neither method wraps the ACS-joining step in a try/catch. If the local ACS session never actually connects (module import failure, `initCallAgent`/`joinRoom` throwing, denied permissions in a way the SDK itself rejects), the backend is left believing the user started/joined while nobody is really connected — occupying a capacity seat and, for Start, blocking the board's one-active-call constraint until a ScrumMaster/Admin manually ends it. Mirrors the exact class of bug the backend review already caught and fixed server-side. **Fixed.** `JoinAcsRoomAsync` is now wrapped in try/catch in both methods; on failure, `boardCallStore.EndCallAsync`/`LeaveCallAsync` (matching Start vs. Join) releases the reservation before rethrowing. [`src/Frontend/Services/BoardCalls/AcsCallInteropService.cs`]
- [x] [Review][Patch] No re-entrancy guard against calling `StartCallAsync`/`JoinCallAsync` while already in a call — confirmed: nothing checks `IsInCall` first. On the JS side, `initCallAgent`/`joinRoom` unconditionally create a new `CallClient`/`CallAgent`/`call` without disposing or hanging up any previous one, and the C# side creates a new `DotNetObjectReference` without disposing the prior one — both leak. **Fixed.** `StartCallAsync`/`JoinCallAsync` now guard with `if (IsInCall) throw new InvalidOperationException(...)`; as defense-in-depth on the JS side too, `initCallAgent` now hangs up any lingering `call` and disposes any lingering `callAgent` before creating new ones. [`src/Frontend/Services/BoardCalls/AcsCallInteropService.cs`, `src/Frontend/WebApp/js-src/acsCallInterop.js`]
- [x] [Review][Patch] `EndLocalSessionAsync` has no error handling around the JS `leaveCall()` invocation — confirmed: if `_module.InvokeVoidAsync("leaveCall")` throws, the whole method throws before `IsInCall`/`IsAudioAvailable`/`IsVideoAvailable` are reset and before `boardCallStore.LeaveCallAsync`/`EndCallAsync` ever runs, leaving both local flags and the backend call record stuck. **Fixed.** The JS call is now wrapped in try/catch (failure logged/ignored); the local flags reset and the backend Leave/EndCallAsync call always still run regardless — the backend is the source of truth for other participants. [`src/Frontend/Services/BoardCalls/AcsCallInteropService.cs`]
- [x] [Review][Patch] `DisposeAsync` never hangs up the call if one is still active — confirmed: it disposes `_selfReference`/`_module` unconditionally but never invokes the JS `leaveCall`. If a component holding this service is disposed while `IsInCall` is true (e.g. navigating away mid-call), the ACS call is never hung up and camera/mic tracks stay live. **Fixed.** `DisposeAsync` now invokes the JS `leaveCall` (best-effort, try/catch) when `IsInCall` before disposing `_module`/`_selfReference`. [`src/Frontend/Services/BoardCalls/AcsCallInteropService.cs`]
- [x] [Review][Patch] `joinRoom`'s returned `videoAvailable` doesn't reflect whether a camera device actually exists — violates AC4 ("or has no such device, they can still join... view-only"). Confirmed: `videoOptions`/`localVideoStream` are only created `if (access.video && cameras.length > 0)`, but the function unconditionally returns `videoAvailable: access.video` — if the user granted camera permission but has zero camera devices, this comes back `true` even though no video stream was attached, so `AcsCallInteropService.IsVideoAvailable` (and eventually Task 8's UI) is told video is available when it isn't. **Fixed.** Now returns `videoAvailable: access.video && !!videoOptions`. [`src/Frontend/WebApp/js-src/acsCallInterop.js`]
- [x] [Review][Patch] `deviceManager.askDevicePermission` isn't wrapped in a try/catch — if it throws (unsupported/insecure context, no media hardware at all in some browser configurations) rather than resolving to `{audio: false, video: false}`, the whole join aborts instead of degrading gracefully, undermining AC4's intent. **Fixed.** Wrapped in try/catch, defaulting to `{audio: false, video: false}` on failure. [`src/Frontend/WebApp/js-src/acsCallInterop.js`]
- [x] [Review][Patch] `attachRenderer` can leave stale DOM nodes when the same `videoElementId` container is reused for a different `streamId` (e.g. a video tile whose occupant changes) — confirmed: it only disposes any prior renderer keyed by the *same* `streamId`, never clears the container's existing children before appending the new view. **Fixed.** Now calls `container.replaceChildren()` before appending the new view. [`src/Frontend/WebApp/js-src/acsCallInterop.js`]
- [x] [Review][Patch] `subscribeToRemoteParticipant` only attaches an `isAvailableChanged` listener to the video streams present *at subscription time* — confirmed: streams added later (e.g. a screen-share stream starting mid-call) trigger `videoStreamsUpdated` once at add-time (which does call `notifyParticipantsChanged`), but never get their own `isAvailableChanged` listener, so a later toggle on that specific stream's availability is silently missed. **Fixed.** The `videoStreamsUpdated` handler now subscribes `isAvailableChanged` on every newly-added stream too (extracted into a shared `subscribeToVideoStream` helper). [`src/Frontend/WebApp/js-src/acsCallInterop.js`]
- [x] [Review][Patch] `BoardCallStore.LoadActiveCallAsync` leaves `ActiveCall` stale on failure after `BoardId` has already been reassigned — confirmed: `BoardId = boardId;` runs before the `await boardCallApi.GetActiveCallAsync(...)` that can throw, so a failed load (e.g. switching boards while offline) leaves `BoardId` pointing at the new board while `ActiveCall` still holds the previous board's call data. **Fixed.** `ActiveCall` is now also reset to `null` at the same point `BoardId` is reassigned, before the awaited call. [`src/Frontend/Services/BoardCalls/Stores/BoardCallStore.cs`]
- [x] [Review][Patch] `BoardActionSyncKey` gives `CallStarted`/`CallParticipantsChanged`/`CallEnded` the same bare `call:{id}` key — confirmed by reading the file. Unlike Task's `TasksReordered`/`TaskCommentsCountChanged`/`TaskAttachmentsCountChanged` (each with its own sub-key suffix), all 3 call types share one key, so `BoardActionsSyncGuard`'s single-timestamp-per-key dedup treats them as one ordered stream. Because `CallParticipantsChanged` is uniquely driven by an async, potentially-delayed, at-least-once Event Grid webhook (unlike Task's synchronous same-request dispatch), a delayed participants-changed notification can advance the shared key's timestamp past a legitimate, not-yet-applied `CallEnded` (or vice versa) for the same call, causing the guard to silently drop it. (`TaskCreated`/`TaskUpdated`/`TaskDeleted` share a bare key too, but that's lower-risk since Task events aren't subject to independent async redelivery the way the Event Grid-driven call events are.) **Fixed.** Each call notification type now gets its own sub-key, matching the existing sub-key convention: `call:{id}:started` / `call:{id}:participants` / `call:{id}:ended`. [`src/Frontend/Services/Boards/BoardActionSyncKey.cs`]
- [x] [Review][Patch] esbuild's bundle script omits `--minify` — `wwwroot/js/acsCallInterop.bundle.js` ships the full unminified `@azure/communication-calling` + `@azure/communication-common` dependency graph. **Fixed.** Added `--minify`; verified the bundle shrank from 7.3 MB to 5.3 MB. [`src/Frontend/WebApp/package.json`]
- [x] [Review][Patch] Inconsistent `CancellationToken` usage on `IAcsCallInteropService` — `StartCallAsync`/`JoinCallAsync`/`LeaveCallAsync`/`EndCallAsync` accept one, but `ToggleMicAsync`/`ToggleCameraAsync`/`StartScreenShareAsync`/`StopScreenShareAsync`/`AttachRendererAsync` don't, and even where accepted it's never forwarded into the underlying `IJSObjectReference.InvokeAsync`/`InvokeVoidAsync` calls (which do support cancellation). **Fixed.** All methods now accept a `CancellationToken ct = default` and forward it into the corresponding `InvokeAsync`/`InvokeVoidAsync` overload. [`src/Frontend/Services.Abstractions/BoardCalls/IAcsCallInteropService.cs`, `src/Frontend/Services/BoardCalls/AcsCallInteropService.cs`]
- [x] [Review][Patch] Dockerfile's early `package.json`/`package-lock.json` `COPY` provides no actual layer-caching benefit as ordered — `npm ci && npm run build` only runs *after* `COPY . .`, which already invalidates the cache on any source change, unlike the analogous `.csproj`-then-`dotnet restore` pattern earlier in the same file that genuinely benefits from the split. **Fixed**, correctly this time: `npm run build` still needs `js-src/acsCallInterop.js` (only present after `COPY . .`), so only `npm ci` (which needs just `package.json`/`package-lock.json`) was moved earlier, before `COPY . .`; `npm run build` stays after. Verified via `docker build` twice — the `npm ci` layer showed `CACHED` on the second, no-source-change run. [`src/Frontend/WebApp/Dockerfile`]

- [x] [Review][Defer] `StartCallAsync`/`JoinCallAsync` responses can race a concurrent real-time state change (a board switch, or another admin's `CallEnded` arriving while the response is in flight) and overwrite fresher `ActiveCall` state — the board-switch half is straightforward to guard (capture the requested `BoardId` and only apply if it still matches on completion) and worth doing alongside the `LoadActiveCallAsync` fix above; the narrower "call ended by someone else during my own in-flight Join" case would need a generation counter or similar to fully close and is deferred as a low-probability, largely self-correcting edge case.
- [x] [Review][Defer] `getParticipantId`'s fallback chain (`communicationUserId` → `.id` → `displayName` → `""`) can collide on `""` if a participant somehow has none of those, and the `"local"` sentinel could theoretically collide with a real participant whose id happens to be that literal string. Neither is realistically reachable given this app only ever provisions ACS Communication User identities (always populates `communicationUserId`) and ACS ids are structured strings, never the literal word "local" — narrow theoretical hardening, not a live bug.
- [x] [Review][Defer] Node.js install via `curl -fsSL https://deb.nodesource.com/setup_22.x | bash -` is unpinned (only pins the major version) and the piped script isn't checksum-verified — a real reproducibility/supply-chain hardening opportunity, but this is the exact snippet the story itself prescribed for this task; not unilaterally changing what the spec dictated without confirming first.
- [x] [Review][Defer] The bundle filename (`acsCallInterop.bundle.js`) is a magic string duplicated across `package.json`'s `outfile`, `AcsCallInteropService._modulePath`, and `.gitignore`'s pattern, with no compile-time link between them — a maintainability nit, not worth a shared-constants mechanism at this scale.
- [x] [Review][Defer] Remote participants' event listeners (`stateChanged`, `videoStreamsUpdated`, `isAvailableChanged`) are never explicitly unsubscribed via `.off(...)` when a participant leaves — likely garbage-collected along with the departed participant object regardless; low-priority hardening.
- [x] [Review][Defer] `IAcsCallInteropService.RemoteParticipantsChanged` has no documented subscribe/unsubscribe discipline — a standard Blazor `OnInitialized`/`Dispose` concern for whichever Task 8 component subscribes to it, not a defect in Task 6/7's own code.

### Findings dismissed as noise or already-decided
- Inconsistent error handling in `BoardCallStore` (`LoadActiveCallAsync` catches and sets `ErrorMessage`; `StartCallAsync`/`JoinCallAsync`/`LeaveCallAsync`/`EndCallAsync` don't) — matches `BoardDetailsStore`'s own established mixed convention exactly (page-load methods catch-and-set-`ErrorMessage`; user-action methods propagate for the caller to catch, matching how `TaskAttachment.razor` wraps `Store.UploadAttachmentAsync` in its own try/catch with a Toast). Not an inconsistency introduced here.
- Asymmetric `ActiveCall` clearing (`EndCallAsync` clears it, `LeaveCallAsync` doesn't) — already a deliberate, documented decision from Task 6 (the leaving user no longer needs the participant list once they've left; left to Task 8's UI to react to).
- `GetActiveCallAsync`'s null-content handling "assumes a backend contract not verified in this diff" — verified already in the prior backend-only review round (the `BoardCallsController.GetActive` action returns `Ok(result)` with a nullable `BoardCallDto`, confirmed 200-with-null-body); simply out of this frontend-scoped diff's view.
- No ACS token/session expiry handling (`AcsCallCredentialsDto.ExpiresOn` never consulted) — already explicitly tracked as a next-story candidate in Dev Notes §9, not a gap in this task's scope.

### Review Findings — Full story review (post-Task-8)

_Code review of the entire story end to end (all 8 tasks, backend + frontend), 2026-07-25, requested after manual testing surfaced real issues. Blind Hunter + Edge Case Hunter + Acceptance Auditor, all three briefed on 3 user-reported findings and asked to root-cause them, not just re-scan generically. All three independently converged on the same root causes for both reported bugs._

**Process note**: the first diff constructed for this review omitted the two new, still-untracked component directories (`BoardCallIndicator`, `BoardCallPanel`) — `git diff` against a commit doesn't include untracked files. The Acceptance Auditor and Blind Hunter both caught this gap themselves and read the working-tree files directly to investigate the reported bugs; a corrected diff (including these files via `git diff --no-index`) was built and used for the remainder of the review. Noted here so the gap isn't repeated in a future review round.

- [x] [Review][Patch] **Root cause of "can't turn off camera during a call" — two compounding bugs, both confirmed by reading the code, all 3 reviewers converged.** (1) DOM ownership conflict: `attachRenderer` in `acsCallInterop.js` called `container.replaceChildren()` + `appendChild(view.target)` directly on the same `<div id="...">` that `BoardCallPanel.razor` also rendered Blazor-owned children into (the placeholder and `.call-tile-label` span) — JS interop silently deleted nodes Blazor's render tree still believed existed, desyncing the two on the next Blazor re-render. (2) Missing detach: `toggleCamera(false)` called `call.stopVideo()` but nothing ever disposed the `VideoStreamRenderer`/view `attachRenderer` had created, so the last rendered frame stayed visually attached even after the SDK-level track stopped. **Fixed**: restructured `BoardCallPanel.razor`'s tile markup so the JS-render target is a dedicated, empty `.call-tile-video` div that Blazor never puts children into (placeholder/label are now siblings, not children, of that div) — this alone fixes the DOM-desync half. Added a new `detachRenderer(streamId)` export in `acsCallInterop.js` (+ `IAcsCallInteropService.DetachRendererAsync`), and reworked `BoardCallPanel`'s `OnAfterRenderAsync` to track which stream IDs are currently attached and explicitly attach/detach on each actual transition (also closes a related, not-user-reported gap the same 2 reviewers found: the same missing-detach bug applied symmetrically to *remote* participants losing video, not just the local camera). [`src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor`, `src/Frontend/WebApp/js-src/acsCallInterop.js`, `src/Frontend/Services.Abstractions/BoardCalls/IAcsCallInteropService.cs`, `src/Frontend/Services/BoardCalls/AcsCallInteropService.cs`]
- [x] [Review][Patch] **Root cause of the "Join call" hover styling bug — CSS specificity collision, all 3 reviewers converged on the exact same explanation.** `.btn:hover:not(:disabled)` and `.btn-primary:hover:not(:disabled)` have identical specificity (0,3,0) but set different properties (`background` vs `filter`) — both applied simultaneously, so hovering "Join call" (`class="btn btn-primary"`) got the generic neutral-gray hover background (meant for the plain `.btn` style) dimmed by the primary button's `filter`, instead of a darkened primary color. **Fixed**: scoped the generic rule to `.btn:hover:not(:disabled):not(.btn-primary)`, and gave `.btn-primary:hover` its own explicit `background` so `filter` has the correct color to dim. [`src/Frontend/WebApp/Components/BoardCallIndicator/BoardCallIndicator.razor.css`]
- [x] [Review][Decision] **The call surfaces as an embedded panel on the board page rather than a separate page — confirmed spec-silent** (no AC or Dev Note ever specified page vs. panel; this was an implementation choice made under-spec during Task 8, not a documented decision). Blind Hunter additionally found a *concrete bug* this design caused: `Board.razor` is a single route reused across different boards (only `OnParametersSetAsync` fires on a board switch, not `Dispose()`), so a user mid-call on Board A who switched to Board B kept their ACS session connected to Board A's call while the still-rendered `BoardCallPanel` began issuing Leave/End/Toggle actions scoped to Board B's `BoardId` instead — a real cross-board state confusion. **Resolved per your explicit choice** (leaving the call page ends the call, matching a typical Zoom/Meet tab): added a new route `/boards/{BoardId:guid}/call` (`BoardCall.razor`) that owns the entire call lifecycle via its own mount/dispose — `OnParametersSetAsync` triggers Start or Join based on `IBoardCallStore.ActiveCall`, `DisposeAsync` calls `LeaveCallAsync` if still connected. `BoardCallIndicator`'s Start/Join buttons now navigate to this route instead of calling `IAcsCallInteropService` directly. `Board.razor` no longer renders `BoardCallPanel` inline and no longer needs `IAcsCallInteropService` injected at all. This also fully closes the cross-board bug Blind Hunter found, since Blazor's router properly disposes a component when navigating to a genuinely different route (unlike same-route parameter changes). [`src/Frontend/WebApp/Pages/Boards/BoardCall.razor` (new), `src/Frontend/WebApp/Components/BoardCallIndicator/BoardCallIndicator.razor`, `src/Frontend/WebApp/Pages/Boards/Board.razor`]
- [x] [Review][Patch] `LeaveBoardCallCommand` never wrote `LeftAt` itself, relying entirely on the async `CallParticipantRemoved` webhook — an asymmetry with Start/Join's synchronous reservation fix that reintroduced a capacity-blocking gap in the other direction: a seat stayed counted as occupied (blocking a new join at the `MaxParticipants` cap) until the webhook eventually confirmed the departure. **Fixed.** `LeaveBoardCallCommand` now synchronously marks `LeftAt`, publishes the `CallParticipantsChanged` notification itself (mirroring `RecordCallParticipantLeftCommand`'s shape), and calls `EndCallIfEmptyAsync` — mirroring the same explicit decision already made for Start/Join. The webhook's own active-only lookup already tolerates the resulting redundant/duplicate delivery as a safe no-op. **Note**: this further extends AC5's literal text ("driven exclusively by Azure Event Grid webhooks") being overridden by your prior capacity-race decision — now true for Leave too, not just Start/Join; worth updating AC5's wording in a future pass since two of the three lifecycle transitions no longer match it literally. [`src/Backend/Application/Features/BoardCalls/Commands/LeaveBoardCallCommand.cs`]
- [x] [Review][Patch] `BoardCallEventsController`: one malformed event's JSON payload inside an otherwise-valid batch could throw uncaught from `ToObjectFromJson`, aborting the entire request (500) and causing Event Grid to retry the whole batch, including events already successfully processed. The existing `JsonException` guard only covered `EventGridEvent.ParseMany` failing on the whole body, not an individual event's `Data` deserialization. **Fixed.** Extracted a `TryParseParticipantEvent` helper wrapping the per-event deserialization in its own try/catch — a malformed individual event is now logged and skipped, the rest of the batch still processes. [`src/Backend/Presentation/Controllers/BoardCallEventsController.cs`]
- [x] [Review][Patch] `AcsCallInteropService.GetModuleAsync`'s lazy JS-module import (`_module ??= await ...`) had a narrow race: two interop calls invoked concurrently before the first import resolved (e.g. two toggle buttons clicked in quick succession) could each trigger their own `import()`, leaking one `IJSObjectReference` proxy. **Fixed.** Caches the import as a `Task<IJSObjectReference>` (not just the resolved value), so concurrent callers all await the same in-flight import. [`src/Frontend/Services/BoardCalls/AcsCallInteropService.cs`]

- [x] [Review][Defer] `BoardCallPanel`'s `OnAfterRenderAsync` re-evaluated attach/detach on every render, including ones unrelated to video state (mic toggle, remote-participant metadata update) — mostly resolved as a side effect of the camera-toggle fix above (attach/detach now only acts on an actual state transition, tracked via `_attachedStreamIds`), but noted here since it wasn't the primary target of that fix.
- [x] [Review][Defer] A board (or an entire workspace) can be soft-deleted while it has an active call — nothing calls `IBoardCallLifecycleService.EndCallAsync` first, so the ACS room (and any still-connected participants) is never released; a genuine resource leak. Fixing this properly means `DeleteBoardCommand`/`DeleteWorkspaceCommand` (Application layer) checking for and ending any active board call(s) before soft-deleting — a real but moderately-scoped change, out of this pass.
- [x] [Review][Defer] The participant-capacity check retains a narrower residual race after the earlier synchronous-reservation fix: two concurrent *new* joins can still both read the active-participant count before either's reservation row commits (no DB-level constraint caps active participants per call, only per-user-per-call uniqueness). The race window shrank from "the entire ACS round-trip" to "a DB read-then-write gap" — consistent with your original decision to accept a narrow race rather than add a Hangfire-style job — but it isn't literally zero. Precision update to the existing entry in `deferred-work.md`.
- [x] [Review][Defer] Corrected understanding of the already-tracked `EnsureUserIdentityAsync` concurrent-identity race: two simultaneous requests each generate a *different* new ACS identity via `CreateUserAsync`, so the unique index on `AcsCommunicationUserId` does **not** actually fire (each value is independently unique) — the real failure mode is a silent last-writer-wins `UPDATE` on the same `User` row (whichever `SaveChangesAsync` commits last simply overwrites the other's value), not a unique-constraint conflict as previously documented. Precision update to the existing entry in `deferred-work.md`.
- [x] [Review][Defer] `StartBoardCallCommand`/`JoinBoardCallCommand`'s failure-recovery `catch` blocks call `SaveChangesAsync` again (to persist the rollback) — if that cleanup save itself throws, the original exception is masked and `DeleteRoomAsync`/rethrow never run, leaking the ACS room. Narrow, compounding-failure scenario (a save fails, and the compensating save also fails).
- [x] [Review][Defer] `BoardCallStore.LoadActiveCallAsync`'s GET isn't sequenced against concurrent push notifications — if a `CallStarted`/`CallEnded` notification arrives while the initial load is still in flight, the (now-stale) GET response can overwrite the fresher real-time state. Narrow race, only matters in the brief window right after a board first loads.

### Findings dismissed as noise or already-decided (full story review)
- `BoardCallEventsController` missing `[Authorize]`/signature verification, and reliance on the shared static `X-Internal-Api-Key` — already reviewed in the Task 4/5 round: matches `InternalBoardsController`'s actual sibling convention (protected by `InternalApiKeyMiddleware`, not `[Authorize]`), and the shared-key concern is already tracked in `deferred-work.md`. Re-raised by a fresh reviewer without that history, not a new finding.
- A batch mixing a `SubscriptionValidationEvent` with real participant events would drop the real ones — already reviewed and dismissed as not reachable in practice (Azure's validation handshake fires once, standalone, never bundled with live events).
- `BoardCallIndicator`'s hardcoded `MaxParticipants = 4` duplicating the backend constant — already documented and accepted (no DTO currently exposes the value).
- `UnitOfWork`'s unique-violation → `ConflictException` translation having a wider blast radius than just `BoardCalls` — already reasoned through in the Task 4/5 round; a strict improvement over the previous opaque-500 fallback for any unique-constraint violation anywhere in the app, not a regression.

## Dev Notes

### §1 — Entity shape (as implemented + required additions)

```csharp
// Domain/Entities/BoardCall.cs — ADD AcsRoomId
public class BoardCall : BaseEntity<Guid>
{
    public required Guid BoardId { get; set; }
    public required Guid StartedByUserId { get; set; }
    public required string AcsRoomId { get; set; }   // NEW
    public DateTimeOffset StartedAt { get; set; }
    public DateTimeOffset? EndedAt { get; set; }

    public Board? Board { get; set; }
    public ICollection<BoardCallParticipant> Participants { get; set; } = [];
}

// Domain/Entities/User.cs — ADD AcsCommunicationUserId
public class User : BaseEntity<Guid>
{
    public required Guid AzureAdObjectId { get; init; }
    public required string Email { get; set; }
    public string? DisplayName { get; set; }
    public string? AvatarUrl { get; set; }
    public string? AcsCommunicationUserId { get; set; }   // NEW — provisioned lazily by IAcsCallService.EnsureUserIdentityAsync
}
```
`BoardCallParticipant` is unchanged from the original implementation.

### §2 — Migrations

`0020_CreateBoardCallTables.sql` gains the `AcsRoomId` column + its unique index (amend in place — safe, since it has not been applied anywhere yet). `0021_AddAcsCommunicationUserIdToUsers.sql` is new (shown in Task 1) — `Users` already exists and has prior migrations, so this must be a fresh script, never folded backward into an earlier one.

### §3 — Why the entire Hangfire/presence-tracker/dedicated-hub mechanism is gone

The original Task 5 existed to solve one problem: *how do we know when the last person has left a call, tolerating a brief disconnect?* That required us to build presence detection ourselves (`IBoardCallPresenceTracker` Singleton + `OnDisconnectedAsync` + a 20-second Hangfire-scheduled idempotent check) because a hand-rolled SignalR hub has no built-in notion of "call membership," only "connection membership." **ACS already solves this** — it tracks real call/participant state itself and emits `CallParticipantRemoved` via Event Grid on its own timing. We do not control or need to reimplement that timing; `RecordCallParticipantLeftCommand` → `IBoardCallLifecycleService.EndCallIfEmptyAsync` is the entire replacement, and it is simpler specifically *because* it's reactive to an authoritative external signal instead of something we had to detect ourselves.

### §4 — Access control (unchanged)

`EnsureCanStartCallAsync` (ScrumMaster/Admin, added to `IBoardAccessService`) and reuse of `EnsureCanViewBoardAsync` (any member, for join/leave/get) are unaffected by the pivot — see the original architecture-spine references. `BoardRolePermissions.CanStartCall` still needs adding.

### §5 — No plan gating for *access*; the participant *cap* is a different axis

Per the standing decision, the call feature itself is **not** gated by subscription plan (unlike `board.export`). The 4-participant **cap**, however, is explicitly designed to become a per-plan `Limits` entry later (mirroring `MaxBoardsPerWorkspace`/`MaxMembersPerWorkspace` in `PlanOptions`/`appsettings.json` — a numeric `Limits` field, not a `Features` list entry, since it's a capacity constraint, not a feature switch). `BoardCallConstants.MaxParticipants` is deliberately the *only* place the number `4` appears, so that future work is: add `MaxCallParticipants` to `PlanLimitsDto`, replace the constant reference in `JoinBoardCallCommandHandler` with `entitlementService`'s resolved plan limit. Nothing else in this story should hardcode participant-count logic anywhere else.

### §6 — Event Grid subscription setup is an Azure-portal/CLI step, not code in this repo

The Event Grid → webhook wiring requires an **Event Grid System Topic on the ACS resource with a Webhook subscription pointing at `/api/internal/board-call-events`** — this is Azure infrastructure configuration (portal, CLI, or a future IaC pass), not something DbUp/EF/docker-compose can express today. The architecture spine already flags "no deployment topology/IaC exists" as a standing open item; this story adds one more thing that will eventually need to live there. For now, document the manual `az eventgrid` CLI command (or Portal steps) needed to create the subscription in the story's implementation notes once Task 5 is built.

### §7 — Local dev/testing: exposing the webhook endpoint

`docker-compose`/`dotnet run` alone cannot receive real Azure Event Grid deliveries, since there's no public HTTPS endpoint for a laptop. To test this end-to-end locally:
1. Run the API locally (`dotnet run` in `Presentation`, or via `docker-compose`).
2. Expose it with a tunnel — either `ngrok http 8080` or Visual Studio/`devtunnel` (`devtunnel host -p 8080 --allow-anonymous`) — and note the resulting public HTTPS URL.
3. Point the Event Grid subscription's webhook endpoint at `https://<tunnel-host>/api/internal/board-call-events` (include the `X-Internal-Api-Key` header in the subscription's delivery configuration — Event Grid supports custom delivery headers).
4. Re-run the `SubscriptionValidationEvent` handshake (Event Grid triggers this automatically on subscription creation) — confirm the controller responds correctly before relying on real event delivery.
5. Remember to update the tunnel URL if it changes between sessions (free ngrok URLs are not stable across restarts; `devtunnel` persistent tunnels avoid this).

### §8 — ACS role mapping

| `BoardRoleDto` | ACS `ParticipantRole` |
| --- | --- |
| `ScrumMaster`, `Admin` | `Presenter` |
| `User` | `Attendee` |

This is enforced at both layers deliberately: our own CQRS checks decide *whether* someone can start/join at all; the ACS role additionally constrains what they can do *inside* the room (e.g. only a `Presenter` can remove another participant at the ACS layer) — defense in depth, not redundant.

### §9 — Token lifetime (explicitly deferred, not forgotten)

`IssueTokenAsync` issues a single ACS access token at join time with no refresh mechanism. This is intentional for this story's scope (bounded Daily-Scrum-length calls) — **but implementing token refresh is a strong candidate for a near-future story**, since ACS tokens do expire and a sufficiently long call (or a call left open in a background tab) will eventually lose its token. Flagging this explicitly so it isn't lost: **next-story candidate — ACS token refresh before expiry.**

### §10 — Testing

No test project exists in this solution (`project-context.md`) — do not add test files or invent a framework. `dotnet build` is the verification gate, consistent with how Tasks 1–2 were validated.

### Project Structure Notes

- `Infrastructure/BoardCalls/AcsCallService.cs` and `Infrastructure/DI/Modules/AcsModule.cs` follow the exact same Infrastructure-implements/Application-declares split as every other external-facing service (Blob, Stripe, Service Bus) — no new pattern introduced here, just a new instance of the existing one (AD-1).
- `src/Frontend/WebApp/js-src/` is a **new** top-level source folder, distinct from `wwwroot/js/` (build *output*) — do not put the esbuild entry point inside `wwwroot`, and do not check the generated `wwwroot/js/acsCallInterop.bundle.js` into source control expectations without also documenting the build step (add `wwwroot/js/*.bundle.js` to `.gitignore` if the team decides build artifacts shouldn't be committed — confirm this convention when Task 7 is implemented, it wasn't decided in this pass).
- `Application/Common/Enums/CallParticipantRole.cs` exists specifically to keep the Azure SDK's own role type out of the Application layer — check whether `Application/Common/Enums/` already exists as a folder before creating it; if the codebase's existing convention puts small enums elsewhere (e.g. `Domain/Enums/`), follow that instead and note the deviation.

### References

- [Source: `_bmad-output/project-context.md`], [Source: `_bmad-output/planning-artifacts/architecture/architecture-TaskTracker-2026-07-25/ARCHITECTURE-SPINE.md`] — AD-1 (dependency direction — `IAcsCallService` interface in Application, SDK usage confined to Infrastructure), AD-3 (Shared/Contracts wire authority — `AcsCallCredentialsDto` wraps ACS SDK types, never leaks them), AD-5 (DbUp is schema authority — both migrations follow numbered-script convention), AD-6 (error contract — the new webhook controller is a plain HTTP controller and gets full `GlobalExceptionHandler` coverage, unlike a SignalR hub; this pivot introduces no new instance of that known gap), AD-7 (explicit resource-scoped authorization, extended with `EnsureCanStartCallAsync` — unchanged by the pivot), AD-9 (Store-only frontend call chain — `BoardCallIndicator`/`BoardCallPanel` still must not bypass the Store)
- [Source: `src/Backend/Infrastructure/DI/Modules/BlobModule.cs`] — pattern to mirror for `AcsModule.cs`'s client registration
- [Source: `src/Backend/Presentation/Middlewares/InternalApiKeyMiddleware.cs`] — the `/api/internal` guard the new webhook controller reuses
- [Source: `src/Frontend/WebApp/wwwroot/js/fileDownload.js`] — existing JS interop precedent (dynamic-import mechanics only; this feature's bundling/stateful-module concerns are new)
- [Source: `src/Frontend/WebApp/Dockerfile`] — base image confirmed to have no Node.js; the npm build step must install it explicitly
- Web (current at time of writing, verify again before implementation — Azure SDKs ship frequently): `Azure.Communication.Identity` 1.3.1, `Azure.Communication.Rooms` 1.2.0, `Azure.Messaging.EventGrid` 5.0.0, `@azure/communication-calling` 1.43.1 (npm). ACS Rooms + Event Grid: `CallParticipantAdded`/`CallParticipantRemoved` confirmed as real, current event types delivered through Event Grid's standard voice/video calling event mechanism (not Call-Automation-only). ACS group calls/Rooms support up to 350 participants and a 30-hour max call duration.

## Change Log

- **2026-07-25 — Architectural pivot (specification):** Replaced the custom P2P WebRTC mesh design (dedicated `BoardCallHub`, hand-rolled SDP/ICE signaling over SignalR, STUN/TURN via Open Relay) with **Azure Communication Services** (Identity + Rooms + `@azure/communication-calling`). Participant presence is now driven by Azure Event Grid webhooks instead of a custom Hangfire grace-period job. Tasks 3–8 rewritten; Tasks 1–2 retained with two required schema additions (`BoardCall.AcsRoomId`, `User.AcsCommunicationUserId`). Introduces a new frontend build-tooling dependency (esbuild + `package.json`) that did not exist in this repo before.
- **2026-07-25 — Task 1–2 ACS additions implemented:** `BoardCall.AcsRoomId` added and `0020_CreateBoardCallTables.sql` amended in place (never applied to any database). `User.AcsCommunicationUserId` added via new `0021_AddAcsCommunicationUserIdToUsers.sql`. `AcsCallCredentialsDto` added to `Shared/Contracts`. `dotnet build` verified 0 errors. Tasks 3–8 remain unimplemented.
- **2026-07-25 — Code review of Task 1/2, findings resolved:** 2 decisions made (`BoardCall`/`BoardCallParticipant` FKs now `Cascade` on Board/BoardCall delete, matching `Column`/`Task`'s sibling convention; `CallStartedPayload`/`CallParticipantsChangedPayload` now embed full DTOs instead of bare IDs, matching `TaskCreatedPayload`/`CommentAddedPayload`), 4 patches applied (`BoardCallConstants` extracted, index naming `UX_`→`IX_`, 4 new `CHECK` constraints added, `BoardRepository.SoftDeleteCascadeAsync` extended to cascade to `BoardCalls`/`BoardCallParticipants`). 4 items deferred to `deferred-work.md` (cross-board active-call constraint, ACS/DB partial-failure cleanup, frontend `BoardActionSyncKey` mapping, ACS identity lifecycle metadata) — all out of Task 1/2's scope. `dotnet build` verified 0 errors after all changes.
- **2026-07-25 — Task 3 implemented:** ACS infrastructure service (`IAcsCallService`/`AcsCallService`) built against the real `Azure.Communication.Identity` 1.3.1 / `Azure.Communication.Rooms` 1.2.0 SDK surface (verified via Microsoft Learn API docs before writing the code, not assumed). One signature refinement from the original spec: `IssueTokenAsync` gained a `roomId` parameter so it can return a complete `AcsCallCredentialsDto`. `dotnet build` verified 0 errors, no new warnings.
- **2026-07-25 — Code review of Task 3, findings triaged and deferred:** 8 patches identified (transaction-commit ownership, `CallParticipantRole`/`AcsCallService` folder placement, missing `RequestFailedException` translation, no input validation, whitespace connection-string gap, no logging, `.env.example` newline), 4 deferred, 7 dismissed. Per explicit user decision, **all 8 patches are deferred until after Task 4** rather than applied immediately — they remain as unchecked action items in Task 3's Review Findings section.
- **2026-07-25 — Task 4 implemented:** All 4 CQRS lifecycle commands (`StartBoardCallCommand`, `JoinBoardCallCommand`, `LeaveBoardCallCommand`, `EndBoardCallCommand`) + `GetActiveBoardCallQuery` + `IBoardCallLifecycleService`. Two deliberate deviations from Task 3's still-outstanding review findings: `EndBoardCallCommand` checks for an existing active call before calling `DeleteRoomAsync` (sidesteps the not-yet-idempotent-on-404 gap for this path); `LeaveBoardCallCommand` reads `User.AcsCommunicationUserId` directly instead of calling `EnsureUserIdentityAsync`, avoiding a "leave provisions an identity" side effect — but its own not-yet-idempotent `RemoveParticipantAsync` gap is accepted as-is, per the explicit decision to build Task 4 first and batch-fix Task 3's patches afterward. Two small, necessary additions beyond the original checklist: `BoardCallMappings` (shared DTO projection, matches the `BoardActionPositionMappings` precedent) and `StartOrJoinBoardCallResponse` (the Contracts DTO the commands needed to return). `MaxParticipants` was folded into the existing `Domain/Constants/BoardCallConstants.cs` rather than creating a second identically-named class in Application. `dotnet build` verified 0 errors, no new warnings.
- **2026-07-25 — Task 3's 7 deferred patches applied:** Removed `AcsCallService`'s inline `SaveChangesAsync`/`IUnitOfWork` dependency (transaction ownership now belongs to the calling command handler; `StartBoardCallCommand`/`JoinBoardCallCommand` adjusted accordingly); moved `CallParticipantRole` to `Domain/Enums/`; moved `AcsCallService` to `Infrastructure/Boards/Calls/`; added `RequestFailedException` → `NotFoundException` translation plus idempotent-404 handling on `RemoveParticipantAsync`/`DeleteRoomAsync` (the one patch fixing an actual reachable bug); added `ArgumentException`/`ArgumentOutOfRangeException` guard clauses; hardened `AcsModule`'s connection-string check to reject whitespace; added `ILogger<AcsCallService>` throughout. Patch 8 (`.env.example` trailing newline) was explicitly left unapplied — the user's choice, not an oversight. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new.
- **2026-07-25 — Task 5 implemented:** Event Grid webhook ingestion — `BoardCallEventsController` (subscription-validation handshake + `CallParticipantAdded`/`CallParticipantRemoved` dispatch), `RecordCallParticipantJoinedCommand`/`RecordCallParticipantLeftCommand` (both idempotent under at-least-once delivery), plus the repository reverse-lookup methods both commands needed (`IBoardCallRepository.GetActiveCallByAcsRoomIdAsync`/`GetParticipantAsync`/`AddParticipantAsync`/`UpdateParticipant`, `IUserRepository.GetByAcsCommunicationUserIdAsync`). One security-relevant deviation from the story's literal spec: the controller route is `[Route("internal/board-call-events")]`, not `"api/internal/board-call-events"` — the latter would have doubled the `"api"` prefix (already auto-prepended by `PrefixConventionConfigurator`) and left the endpoint unprotected by `InternalApiKeyMiddleware`'s `/api/internal` check. `SubscriptionValidationResponse`'s exact SDK shape couldn't be verified against live docs, so a local record matching the confirmed JSON contract is used instead of guessing the SDK type. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new.
- **2026-07-25 — Code review of Task 4 & Task 5, findings resolved:** Blind Hunter + Edge Case Hunter + Acceptance Auditor all independently converged on two `[high]` bugs. 1 decision resolved (participant-capacity enforcement was unreliable because `BoardCallParticipant` rows were only ever written asynchronously by the Event Grid webhook — user chose to reverse that design: `StartBoardCallCommand`/`JoinBoardCallCommand` now insert the row synchronously as a reservation, with compensating rollback if the subsequent ACS calls fail, and no new Hangfire/background cleanup job, per the user's explicit direction not to reintroduce the grace-period mechanism Dev Notes §3 removed). 8 patches applied: the ACS-room-leak on auto-end (`BoardCallLifecycleService.EndCallAsync` now calls `DeleteRoomAsync`) — the most consequential fix, since it's the common "everyone just left" path; the explicit-end participant-orphan bug (same method now synchronously closes out every still-open participant row before the room is deleted); a new general unique-violation → `ConflictException` translation in `UnitOfWork.SaveChangesAsync` (Postgres `23505`, since Application can't reference Npgsql/EF types directly per AD-1); the matching ACS-room-leak on a `StartBoardCallCommand` failure after room creation; malformed-Event-Grid-event logging; `EventGridEvent.ParseMany` exception handling (verified `JsonException` as the thrown type before catching it); a stray blank line; and an `EndBoardCallCommand` permission-naming fix (`BoardRolePermissions.CanStartCall` → `CanManageCall`, plus a new `EnsureCanEndCallAsync`). One necessary follow-on fix surfaced while implementing the capacity-race decision: `RecordCallParticipantJoinedCommand`'s old "reactivate a stale row" logic would have raced against the new synchronous reservation and violated the participant table's partial unique index — fixed by switching its lookup to the active-only query and removing the now-unused `GetParticipantAsync` repository method. 3 items deferred to `deferred-work.md` (out-of-order Event Grid delivery, the shared static internal API key, a narrow notification-payload race), 6 dismissed as noise/already-decided/misreads. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new.
- **2026-07-25 — Task 6 implemented:** Frontend contracts, API client, and store — `IBoardCallsApi` (Refit), `IBoardCallApiService`/`BoardCallApiService`, `IBoardCallStore`/`BoardCallStore`, wired into `BoardActionsHubService` and `Services/DI/ServiceCollectionExtensions.cs`. Closed the `BoardActionSyncKey.cs` gap explicitly deferred here from the Task 1/2 review by adding proper `call:{BoardCallId}` sync keys for the 3 call notification types. Found and filled a real gap discovered while implementing this task: **no `BoardCallsController` existed anywhere** — Task 4 built the CQRS commands/query but never gave them an HTTP endpoint, so `IBoardCallsApi` would have had nothing to call. Created `Presentation/Controllers/BoardCallsController.cs` mirroring `TasksController`'s exact shape. Also found that the shared `HandleResponseAsync<T>()` extension treats a null success body as a failure, which doesn't fit `GetActiveCallAsync`'s legitimately-nullable "no active call" response — worked around locally in `BoardCallApiService` rather than changing the shared extension. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new.
- **2026-07-25 — Task 7 implemented:** esbuild bundling + ACS Calling SDK JS interop — `package.json` (added `@azure/communication-common` beyond the story's listed dependency, needed for `AzureCommunicationTokenCredential`), `js-src/acsCallInterop.js`, `AcsCallInteropService`/`IAcsCallInteropService`. Corrected one wrong assumption in the story's own text after verifying against the live ACS Calling SDK reference: the current GA screen-sharing methods are `call.startScreenSharing()`/`call.stopScreenSharing()`, not `startScreenSharingAsync()`/`stopScreenSharingAsync()`. `AcsCallInteropService` orchestrates `IBoardCallStore` rather than `IBoardCallApiService` directly (the story's literal text), avoiding a duplicate Start/Join network call between the interop layer and whatever Task 8 calls on the Store. Both the `npm run build` esbuild step and the updated Dockerfile were verified for real — a full `docker build` ran end-to-end (Node install → `npm ci` → bundle → `dotnet publish` → nginx image), not just written and assumed to work. Resolved the previously-open "should the bundle be gitignored" question from the Task 1/2 review by adding it to `.gitignore`, and documented the local `npm install && npm run build` step in the root `README.md`. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new.
- **2026-07-25 — Code review of Task 6 & Task 7 (frontend), all 13 patches applied:** Blind Hunter + Edge Case Hunter + Acceptance Auditor converged on the same core issue from three angles: `AcsCallInteropService.StartCallAsync`/`JoinCallAsync` had no rollback if the local ACS session failed to connect after the backend reservation already succeeded — the exact class of bug the backend review had already caught and fixed server-side, just recurring on the client. Fixed, plus a re-entrancy guard (`IsInCall` check + JS-side defensive `callAgent`/`call` cleanup), error handling around the JS `leaveCall()` invocation so a messy hangup can't strand local/backend state, and `DisposeAsync` now hangs up an active call before disposing. A real AC4 bug was also caught and fixed: `joinRoom`'s `videoAvailable` didn't check whether a camera device actually existed, only whether permission was granted. `deviceManager.askDevicePermission` is now guarded against throwing. `attachRenderer` now clears its container before appending (stale-DOM-node fix), and new video streams added mid-call (e.g. screen share) now get their own `isAvailableChanged` subscription. `BoardCallStore.LoadActiveCallAsync` no longer leaves `ActiveCall` pointing at a stale board's data on failure. A genuine dedup-correctness bug already shipped in Task 6 was found and fixed: `BoardActionSyncKey` gave all 3 call notification types the same bare `call:{id}` key, which could let a delayed, Event-Grid-driven `CallParticipantsChanged` silently cause the guard to drop a legitimate `CallEnded` for the same call — each type now gets its own sub-key. Also: the esbuild bundle is now minified (7.3 MB → 5.3 MB, verified), `CancellationToken` usage is now consistent and actually forwarded into JS interop calls, and the Dockerfile's `npm ci` was moved before `COPY . .` for a real caching benefit (`npm run build` itself must stay after, since it needs the copied `js-src/` source) — verified via two `docker build` runs, the second showing the `npm ci` layer `CACHED`. 6 items deferred to `deferred-work.md` per explicit instruction to revisit after Task 8, 4 dismissed as noise/already-decided. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new.
- **2026-07-25 — Task 8 implemented (story complete):** `BoardCallIndicator.razor` and `BoardCallPanel.razor`, wired into `Pages/Boards/Board.razor`. Two necessary additions surfaced while wiring the two components together, beyond anything in the Task 7/8 checklist text: (1) `IAcsCallInteropService` gained a general `StateChanged` event — without one, nothing told `BoardCallPanel` that `BoardCallIndicator`'s Start/Join button had just succeeded, since the only existing event (`RemoteParticipantsChanged`) doesn't cover that transition; (2) `acsCallInterop.js` now listens for `call.on("stateChanged")` and invokes a new `[JSInvokable] OnCallDisconnected()` when the ACS call disconnects from the *other* side (e.g. a different admin ending the call for everyone) — without this, AC6 wouldn't actually hold on the client still connected, since nothing detected the remote-initiated disconnect and `IsInCall` would stay stuck `true`. Also corrected the story's own earlier Task 8 checklist text, written before this task was implemented: participant tile labels use `RemoteParticipantInfo.DisplayName` directly rather than cross-referencing `IBoardCallStore.ActiveCall.Participants` for `DisplayName`/`AvatarUrl` — the two lists don't share a common identifier (`RemoteParticipantInfo.Id` is the ACS `communicationUserId`; `BoardCallParticipantDto.UserId` is our internal `Guid`), so that cross-reference was never actually possible; the JS SDK's own `displayName` field already carries a usable name. `MaxParticipants = 4` is a local frontend constant mirroring the backend's — no DTO currently exposes the value, a known and accepted small duplication. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new; `npm run build` re-verified after the JS interop change. **Not independently verified in a live browser** — actually starting/joining a call requires a configured Azure AD tenant and a live Azure Communication Services resource, neither available in this environment; verification here is build-level only (compiles cleanly, JS bundles cleanly), not a confirmed working end-to-end call.
- **2026-07-25 — Full story code review (all 8 tasks), after manual testing surfaced real issues:** requested after you found 3 concrete problems by actually using the feature — this is exactly the kind of integration-level gap build-only verification can't catch. All 3 reviewers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) independently converged on the same root causes for both reported bugs. **Camera-off bug**: a DOM ownership conflict (JS interop's `container.replaceChildren()` was deleting Blazor-owned sibling nodes in the same tile div) plus a missing renderer-disposal call on toggle-off — fixed by giving each video tile a dedicated, JS-exclusive child div and adding a proper attach/detach lifecycle (also closes the same bug for remote participants losing video, which wasn't reported but shares the identical root cause). **Hover CSS bug**: a CSS specificity collision between `.btn:hover` and `.btn-primary:hover` setting different properties without either fully overriding the other — fixed by scoping the generic rule to exclude `.btn-primary` and giving the primary button's hover its own explicit background. **Embedded-panel-vs-separate-page**: confirmed the spec never actually specified this (an implementation choice made under-spec) — and Blind Hunter found a concrete bug it caused (switching boards mid-call didn't end the previous board's call, since `Board.razor` is one reused route whose `Dispose()` never fires on a parameter-only board switch). Per your explicit choice (leaving the call page ends the call), built a new dedicated route/page (`BoardCall.razor`) owning the whole call lifecycle via its own mount/dispose, which also fully closes that cross-board bug. Also fixed two backend gaps this pass surfaced: `LeaveBoardCallCommand` now marks `LeftAt` synchronously (closing a capacity-blocking asymmetry — Join reserved synchronously but Leave didn't release synchronously) and `BoardCallEventsController` now isolates per-event JSON deserialization failures so one malformed event can't abort an entire Event Grid batch. 4 new items deferred to `deferred-work.md` (board/workspace delete not ending an active call; a narrower residual capacity race; a compounding-failure gap in the Start/Join rollback catch blocks; a load-vs-notification race in `BoardCallStore`), plus 2 existing deferred entries corrected for a more precise root cause. 4 findings dismissed as already-reviewed/already-decided (re-raised by fresh reviewers without that history). `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new; `npm run build` re-verified.
- **2026-07-26 — Manual-testing bug reports + full deferred-work sweep:** you simulated a real camera-in-use error and reported six concrete problems, plus asked to resolve everything tracked in `deferred-work.md`. **(1) Raw stack trace in error toasts**: root-caused to Blazor's `JSException.Message` being the JS `Error`'s full `.stack` (message + every frame), not just its `.message` — fixed by routing every JS call site in `AcsCallInteropService` through new `InvokeVoidSafeAsync`/`InvokeSafeAsync<T>` wrappers that strip to the first line before it can reach a Toast. **(2) No pre-join camera/mic preview**: built a new `BoardCallPreview.razor` component (camera preview + independent mic/camera toggles, shown before StartCallAsync/JoinCallAsync is ever called) plus new JS (`initPreview`/`setPreviewCamera`/`disposePreview`) and `IAcsCallInteropService` methods (`StartPreviewAsync`/`SetPreviewCameraAsync`/`StopPreviewAsync`); `initCallAgent`/`joinRoom` now reuse the preview's already-created `CallClient`/`deviceManager`/`localVideoStream` instead of re-requesting permission and recreating them. `BoardCall.razor`'s state machine now shows the preview first, then Connecting, then the panel. **(3) Mic/camera toggle showing wrong default state**: root-caused to `BoardCallPanel` initializing its toggle state from raw device *availability* (`IsAudioAvailable`/`IsVideoAvailable`) instead of what was actually requested — fixed by adding real `IsMicOn`/`IsCameraOn` state to `IAcsCallInteropService`, set from the preview's chosen mic/camera flags at join time (JS's `joinRoom` now takes `micEnabled`/`cameraEnabled` params and mutes/omits video accordingly) and kept in sync on every toggle; `BoardCallPanel` now binds directly to these instead of tracking its own shadow copy. **(4) Participant count not decreasing for the leaver**: root-caused to `BoardActionsSyncGuard` filtering a user's own `CallParticipantsChanged` notification, combined with `BoardCallStore.LeaveCallAsync` never optimistically updating local state — fixed by having it remove the current user from `ActiveCall.Participants` locally right after the backend call succeeds. **(5) No redirect after Leave/End call**: `BoardCall.razor` now subscribes to `IAcsCallInteropService.StateChanged` and navigates back to the board once `IsInCall` transitions from true to false, covering both an explicit Leave/End and a remote "ended for everyone" disconnect. **(6) Local tile showing literal "You"**: `BoardCallPanel` now shows `{ProfileStore.Profile.DisplayName} (You)`, falling back to plain "You" if no display name is set. **Deferred-work sweep**: worked through every item in `deferred-work.md` (see that file's new 2026-07-26 section for the full per-item disposition) — resolved 12 of 14 open items (the `EnsureUserIdentityAsync` race via a new per-user `AcquireDistributedLockAsync` lock shared by `StartBoardCallCommand`/`JoinBoardCallCommand`; the residual join-capacity TOCTOU race via the same lock pattern keyed per-call; ACS room TTL given a 24h safety-net upper bound; out-of-order Event Grid delivery via a new `GetLatestParticipantAsync` lookup; the null-participant-list webhook-notification gap; the Start/Join rollback-catch-could-itself-mask-the-original-exception gap; `BoardCallStore`'s load-vs-notification race via a generation counter (also applied to Start/Join/Leave/End for the broader race the Task 6/7 review had flagged); `getParticipantId`'s fallback-collision risk; and board/workspace delete not ending an active call first, via a new `DeleteBoardCommand`/`DeleteWorkspaceCommand` check plus a new `IBoardCallRepository.GetActiveCallsForWorkspaceAsync`), left 2 consciously unresolved with reasoning recorded inline (the speculative cross-board single-active-call constraint — no AC requires it; the Dockerfile's unpinned Node.js install — it's the exact snippet the story itself prescribed for Task 7), and left the remaining 3 low-value/infra-gap items exactly as previously documented (no ACS emulator; the shared static internal API key, a pre-existing cross-cutting architecture; the bundle-filename magic string and remote-participant `.off()` cleanup). `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new; `npm run build` re-verified. **Still not independently verified in a live browser** — same infra limitation as every prior round (no configured Azure AD tenant / live ACS resource in this environment).
- **2026-07-26 — Immediate follow-up bug reports from the previous round, all fixed:** (1) **"End call for everyone" appeared broken** — clicking it instantly redirected to the board, showed a "Board is not loaded" error toast, and the call kept running server-side. Root-caused to two compounding races, both closed: first, `AcsCallInteropService.EndCallAsync`/`LeaveCallAsync` tore down local state and fired `StateChanged` (triggering `BoardCall.razor`'s auto-navigate-back-to-board from the previous round's fix) *before* the backend request even went out, so the navigation raced and could kill the in-flight request — reordered so the backend call now always completes first. Second, and the actual reason the error said "Board is not loaded": even with that reordering, a *second*, independent race remained — navigating away triggers `Board.razor.OnParametersSetAsync` to call `BoardCallStore.Reset()` (nulling its ambient `BoardId`) concurrently with the still-in-flight Leave/End request reading that same `BoardId`. Root-caused as a genuine Blazor disposal-ordering gotcha (component disposal on navigation isn't guaranteed to complete before the next page's lifecycle methods run), so the fix doesn't try to sequence around it — instead, `IBoardCallStore.StartCallAsync`/`JoinCallAsync`/`LeaveCallAsync`/`EndCallAsync` and `IAcsCallInteropService`'s equivalents now take an explicit `Guid boardId` parameter from the caller (which always knows it unambiguously — `BoardCall.razor`'s own route parameter, now also passed down to a new `BoardCallPanel.BoardId` parameter) instead of trusting the shared, concurrently-mutable `BoardCallStore.BoardId`; `BoardCallStore`'s own state mutations now guard with `if (BoardId == boardId)` so a response that resolves after the store has moved on to a different board (or been reset) is silently dropped rather than misapplied. This fully closes the "Board is not loaded" class of bug, including the separately-reported case where it appeared just from leaving the board page mid-call. (2) **Call panel too tall / no scroll with a single participant** — `.call-tiles`' grid previously stretched a single tile to the full container width (`minmax(180px, 1fr)`), and its 16:9 aspect ratio then made it taller than the viewport with nothing to scroll. Fixed by capping tile width (`minmax(200px, 360px)` + `justify-content: center`, so a lone tile no longer stretches) and making `.call-panel` a bounded flex column whose `.call-tiles` region scrolls independently (`flex: 1 1 auto; min-height: 0; overflow-y: auto`) if content still overflows on a short viewport. (3) **Call controls moved to bottom-center** — mic/camera/screen-share/leave/end-for-everyone were in the top-right `.call-panel-head`; moved into a new `.call-controls` bar at the bottom of the panel, centered, below the video tiles. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new (no JS changes this round, so `npm run build` wasn't re-run). **Still not independently verified in a live browser** — same infra limitation as every prior round.
- **2026-07-26 — Call capacity reworked from a fixed global constant to the board's own membership:** you reported the "N/4 joined" cap as effectively workspace-derived and asked for it to be scoped to board members instead. Confirmed `Domain.Constants.BoardCallConstants.MaxParticipants = 4` was in fact a flat, board-agnostic constant (never workspace-derived, but equally wrong for the same reason: it had no relationship to who could actually be on the board) — removed it entirely. `BoardCallDto` gained a `MaxParticipants` field, populated server-side from a new `IBoardMemberRepository.CountByBoardIdAsync(boardId)` everywhere a `BoardCallDto` is produced (`StartBoardCallCommand`, `JoinBoardCallCommand`, `GetActiveBoardCallQuery`); `JoinBoardCallCommand`'s capacity check now compares the active-participant count against this board-member count instead of the removed constant. `BoardCallMappings.ToDto` gained a required `maxParticipants` parameter; the three call sites that only needed the participant list (`RecordCallParticipantJoinedCommand`/`LeftCommand`, `LeaveBoardCallCommand`) were switched to a new `BoardCallMappings.ToParticipantDtos` helper instead of threading a meaningless placeholder value through just to discard it. On the frontend, `BoardCallIndicator.razor`'s local `MaxParticipants = 4` constant (a duplicate of the backend's, called out as a known small duplication in the previous round's completion notes) is gone — it now reads `activeCall.MaxParticipants` straight off the shared `Contracts.DTOs.BoardCallDto`, so there's exactly one source of truth for this number and it's always current board membership, not a hardcoded number either side could drift from. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new; no JS changed, so `npm run build` wasn't re-run. **Still not independently verified in a live browser** — same infra limitation as every prior round.
- **2026-07-26 — Five bugs from continued manual testing, all fixed:** you hit an actual unhandled crash plus four more UX/feature gaps in the same message. **(1) Unhandled crash** ("Failed to render video stream, this stream is not longer available. Remote participant turned their video off."): `renderer.createView()` in `attachRenderer` can legitimately reject in the race window between checking a stream's availability and the render resolving (the remote participant turns their camera/share off in between) — it wasn't wrapped in a try/catch, so the rejection became a `JSException` that propagated all the way up through `BoardCallPanel.OnAfterRenderAsync`, a Blazor lifecycle method with no error boundary, crashing the entire WASM renderer. Fixed at both layers: `attachRenderer` now catches `createView()` failures and degrades to "no renderer" (`false`) instead of throwing; `BoardCallPanel.SyncRendererAsync` also wraps its attach/detach calls defensively, so no future unexpected interop error can crash the app this way either. **(2) Screen share couldn't be restarted after stopping once**: root-caused to `BoardCallPanel`'s local `_screenSharing` boolean only ever being flipped by our own Start/Stop button clicks — if sharing stopped any other way (most commonly the browser's own native "Stop sharing" bar), the SDK's real `call.isScreenSharingOn` went false but our local flag stayed stuck `true`, so the button kept trying to *stop* an already-stopped share (which fails) and could never successfully *start* one again for the rest of the call. Fixed by adding a real `IsScreenSharing` property to `IAcsCallInteropService`, driven exclusively by a new `call.on("isScreenSharingOnChanged", ...)` JS listener (→ new `[JSInvokable] OnScreenSharingChanged`) rather than being set optimistically by `StartScreenShareAsync`/`StopScreenShareAsync` themselves — this is the same category of bug as `IsMicOn`/`IsCameraOn` before them, now fixed with the same "one real event-driven source of truth" pattern. **(3) Screen share was never actually visible to anyone**: turned out the feature had only ever wired up the start/stop toggle — nothing anywhere rendered the screen-share video stream, for either the local user's own share or a remote participant's, regardless of role. `mediaStreamType` ("Video" vs "ScreenSharing") is now used throughout to distinguish a participant's camera stream from their screen-share stream (previously `findRemoteVideoStream` just grabbed whichever stream happened to be first); `RemoteParticipantInfo` gained `IsScreenSharing`; new full-width `.call-tile-screen` tiles (local + one per currently-sharing remote participant) render above the regular camera grid, using `object-fit: contain` so shared screens/documents aren't cropped like camera tiles are. **(4) Call tiles too small, not using available space**: the previous round's `minmax(200px, 360px)` width cap (added to stop a single tile from becoming absurdly tall) was also preventing tiles from growing to fill available room with 2+ participants. Switched to `minmax(280px, 1fr)` (tiles now stretch to fill space) and moved the "don't get too big" protection from width to `max-height: 46vh` on `.call-tile` instead — a lone participant's tile can now be as wide as the panel while staying height-bounded, and multiple participants' tiles grow to use empty space, both without reintroducing the original no-scroll problem. **(5) Avatars never shown; fallback colors inconsistent with the rest of the app**: call tile placeholders always rendered flat-colored initials, ignoring `AvatarUrl` entirely and using a single fixed color for everyone instead of this app's existing deterministic per-user `AvatarColorHelper` (already used in the sidebar, task assignees, etc.). Fixed for the local tile via `IProfileStore` directly; for remote tiles, added `AcsCommunicationUserId` to `BoardCallParticipantDto` (populated from `User.AcsCommunicationUserId`) so `BoardCallPanel` can correlate `RemoteParticipantInfo.Id` (the ACS SDK's communicationUserId, the only identifier it exposes) back to `BoardCallStore.ActiveCall.Participants` (which carries our own `AvatarUrl`) — closing the "these two participant lists don't share a key" gap called out as a known limitation in earlier rounds. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new; `npm run build` re-verified (acsCallInterop.js changed substantially this round). **Still not independently verified in a live browser** — same infra limitation as every prior round; the exact ACS SDK behaviors relied on here (`mediaStreamType`, `isScreenSharingOnChanged`, `createView()` rejecting on a since-unavailable stream) were checked against the installed `@azure/communication-calling` package's own TypeScript definitions rather than assumed.
- **2026-07-26 — Two more bugs, one a direct regression from the previous round:** **(1) Own camera/screen video invisible to yourself (only other participants' video showed)**: a genuine regression introduced by the previous round's crash fix. `attachRenderer` gained an `if (!stream || !stream.isAvailable) return false;` guard — but `isAvailable` is a `RemoteVideoStream`-only property (confirmed against the SDK's own `.d.ts`); `LocalVideoStream` (used for the local camera and local screen-share tiles) has no such field, so it's always `undefined` → falsy, meaning the guard silently refused to render *any* local stream, 100% of the time, for everyone. Fixed by only checking `!stream` — remote streams are already `isAvailable`-filtered inside `findRemoteVideoStream`'s own lookup, so the extra check on the resolved stream was both redundant for remote and actively harmful for local. **(2) "End call for everyone" didn't force other participants out**: ending a call marks it ended and deletes the ACS Room server-side, but Rooms mainly gate *join* eligibility — deleting one isn't a documented guarantee that it forcibly disconnects a participant's *already-established* live media session, so other participants' clients could just keep running indefinitely. Rather than depend on that ACS behavior, added a new `IBoardCallStore.CallEndedRemotely` event, fired specifically when an incoming `CallEnded` notification matches the client's currently-tracked `ActiveCall` (this only ever reaches non-actors — `BoardActionsSyncGuard` already filters the ender's own notification back to them, and the ender's own client tears down directly through its own `EndCallAsync` call anyway). `AcsCallInteropService` now subscribes to this event and force-hangs-up its local ACS session (the same `EndLocalSessionAsync` used elsewhere) the moment it fires — a reliable, ACS-behavior-independent guarantee that every other participant's client actually leaves. This required converting `AcsCallInteropService` from a primary-constructor class to a regular one (private fields prefixed `_`), since subscribing an instance event handler in the constructor needed a real constructor body. `dotnet build -t:Rebuild` verified 0 errors, 17 pre-existing warnings, none new; `npm run build` re-verified (the `attachRenderer` fix touches `acsCallInterop.js`). **Still not independently verified in a live browser** — same infra limitation as every prior round.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- `dotnet build TaskTracker.sln` — succeeded, 0 errors, 17 pre-existing warnings (initial Task 1–2 implementation, pre-pivot).
- `dotnet build TaskTracker.sln` — succeeded, 0 errors, same 17 pre-existing warnings, none new (ACS pivot additions to Task 1–2).
- `dotnet build TaskTracker.sln` — succeeded, 0 errors, same 17 pre-existing warnings, none new (post code-review fixes).
- `dotnet build TaskTracker.sln` — succeeded, 0 errors, same 17 pre-existing warnings, none new (Task 3, ACS infrastructure service).
- `dotnet build TaskTracker.sln` — succeeded, 0 errors, same 17 pre-existing warnings, none new (Task 4, CQRS lifecycle commands).
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, 17 pre-existing warnings, none new (this session — Task 3's 7 approved patches applied).
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, 17 pre-existing warnings, none new (Task 5, Event Grid webhook ingestion; fixed 3 new CS8602 nullable-reference warnings during implementation via explicit null-pattern checks before this final count).
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (Task 4 & Task 5 code review: capacity-race decision applied + all 8 patches applied).
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (Task 6, frontend contracts/API client/store + the new `BoardCallsController`).
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (Task 7, esbuild bundling + ACS Calling SDK JS interop).
- `npm run build` (from `src/Frontend/WebApp`) — succeeded, produced `wwwroot/js/acsCallInterop.bundle.js` (7.3 MB).
- `docker build -f src/Frontend/WebApp/Dockerfile .` — succeeded end-to-end (Node.js install → `npm ci` → `npm run build` → `dotnet publish` → nginx final image), confirming the updated Dockerfile actually works, not just written and assumed correct.
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (Task 6 & Task 7 code review: all 13 patches applied).
- `npm run build` (from `src/Frontend/WebApp`) — succeeded, bundle shrank from 7.3 MB to 5.3 MB after adding `--minify`.
- `docker build -f src/Frontend/WebApp/Dockerfile .` (run twice) — succeeded both times; the second run showed the `npm ci` layer as `CACHED`, confirming the Dockerfile reorder fix actually gives the intended caching benefit.
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (Task 8, UI components).
- `npm run build` (from `src/Frontend/WebApp`) — succeeded, re-verified after adding the `call.on("stateChanged")` listener to `acsCallInterop.js`.
- **No live browser verification was possible for Task 8** — starting/joining an actual ACS call needs a configured Azure AD tenant and a live Azure Communication Services resource, neither available in this environment. Verification is build-level only.
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (full story code review: camera-toggle fix, hover-CSS fix, separate call page, `LeaveBoardCallCommand`/`BoardCallEventsController` backend fixes).
- `npm run build` (from `src/Frontend/WebApp`) — succeeded, re-verified after adding `detachRenderer` to `acsCallInterop.js`.
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (this session — error-message cleanup, preview screen, mic/camera state fix, leaver participant-count fix, redirect-after-call fix, "(You)" badge).
- `npm run build` (from `src/Frontend/WebApp`) — succeeded, re-verified after adding `initPreview`/`setPreviewCamera`/`disposePreview` and the `micEnabled`/`cameraEnabled` `joinRoom` params to `acsCallInterop.js`.
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (this session — full `deferred-work.md` sweep: distributed-lock races, ACS room TTL, out-of-order Event Grid handling, null-notification-fallback fix, rollback-masking fix, `BoardCallStore` generation counter, `getParticipantId` hardening, board/workspace delete ending active calls).
- `npm run build` (from `src/Frontend/WebApp`) — succeeded, re-verified after the `getParticipantId` fallback hardening.
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (this session — End-call race fix via explicit `boardId` threading, call-panel layout/scroll fix, controls moved to bottom-center). No JS changes this round, so `npm run build` wasn't re-run.
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (this session — call capacity reworked to board-member count; `BoardCallDto.MaxParticipants` added; `BoardCallConstants.MaxParticipants` removed). No JS changed, so `npm run build` wasn't re-run.
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (this session — crash fix, screen-share desync fix, screen-share rendering, tile sizing, real avatars). `npm run build` re-verified — `acsCallInterop.js` changed substantially this round (mediaStreamType-aware stream resolution, isScreenSharingOnChanged listener, defensive createView() handling).
- `dotnet build TaskTracker.sln -t:Rebuild` — succeeded, 0 errors, same 17 pre-existing warnings, none new (this session — fixed the local-stream `isAvailable` regression; added `IBoardCallStore.CallEndedRemotely` to force other participants to hang up when a call ends for everyone). `npm run build` re-verified after the `attachRenderer` fix.

### Completion Notes List

- Tasks 1–2 implemented under the original (pre-pivot) design in an earlier session.
- This session: implemented the two ACS-pivot schema additions flagged in the story revision, and the one new Contracts DTO — nothing else. Tasks 3–8 remain untouched, as scoped by the user's request.
  - `BoardCall.AcsRoomId` (required `string`, max 128) added to the entity, `BoardCallConfiguration` (required, unique index), and amended directly into `0020_CreateBoardCallTables.sql` (safe to amend in place — never applied to any database).
  - `User.AcsCommunicationUserId` (nullable `string`, max 255) added to the entity and `UserConfiguration` (partial unique index, `WHERE "AcsCommunicationUserId" IS NOT NULL`, so multiple users without one don't collide). Landed in a **new** `0021_AddAcsCommunicationUserIdToUsers.sql` rather than amending `0001` — `Users` already has prior applied migrations, so it cannot be retroactively amended (only `0020` could be, since it was never shipped).
  - `Contracts/DTOs/BoardCalls/AcsCallCredentialsDto.cs` added — a plain wrapper record, no `Azure.Communication.*` SDK types referenced (AD-3 compliance; the SDK isn't even a dependency yet, since Task 3 hasn't started).
- Verification: `dotnet build TaskTracker.sln` (0 errors) — no test project exists in this solution (`project-context.md`), so build success remains the gate, consistent with the original Task 1–2 pass.
- Code review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) ran against the full Task 1/2 diff. 2 `decision-needed` findings resolved by the user (FK cascade behavior, payload richness — see Review Findings above), 4 `patch` findings applied, 4 deferred (logged in `deferred-work.md`, all genuinely out of Task 1/2 scope), 7 dismissed (already-decided, working-as-designed, or inconsistent with established codebase conventions). Status kept at `in-progress` rather than `done` — the review was explicitly scoped to Task 1/2 only; Tasks 3–8 remain unimplemented, so the story as a whole is not complete.
- Task 3 implemented: `IAcsCallService`/`AcsCallService` (Infrastructure) + `CallParticipantRole` enum (Application) + `AcsModule` DI wiring + connection-string plumbing. Every Azure SDK call was checked against the actual Microsoft Learn API reference before writing it (constructor overloads, method signatures, the fact that `ParticipantRole`/`CommunicationTokenScope` are extensible `readonly struct`s with static properties, not plain enums) — this avoided several plausible-but-wrong guesses (e.g. `RoomParticipant` takes a `CommunicationIdentifier` in its constructor with `Role` set via object initializer, not a constructor overload with a role parameter). `IssueTokenAsync` gained a `roomId` parameter beyond the original story spec — a necessary correction since the original signature couldn't have populated `AcsCallCredentialsDto.RoomId`. No entitlement/plan-gating check was added (still an explicit non-goal per Dev Notes §5/§7). `dotnet build` — 0 errors, no new warnings.
- Code review of Task 3 ran (Blind Hunter + Edge Case Hunter + Acceptance Auditor): 0 `decision-needed`, 8 `patch`, 4 `defer`, 7 dismissed. **Per explicit user instruction, all 8 patches were left unapplied** so Task 4 could be built first — they remain as unchecked `[ ]` action items in Task 3's Review Findings section, to be applied in a dedicated follow-up pass.
- Task 4 implemented: `StartBoardCallCommand`, `JoinBoardCallCommand`, `LeaveBoardCallCommand`, `EndBoardCallCommand`, `GetActiveBoardCallQuery`, `IBoardCallLifecycleService`/`BoardCallLifecycleService`, `BoardCallMappings`, `StartOrJoinBoardCallResponse`, `EnsureCanStartCallAsync`. Built directly against Task 3's *current* (pre-patch) code — e.g. `Application.Common.Enums.CallParticipantRole`, not the `Domain.Enums` location the deferred patch will move it to; these `using` statements will need a small follow-up update once that patch lands. Two handlers were deliberately designed to sidestep Task 3's known idempotency gaps without touching `AcsCallService` itself (see Change Log for the reasoning) — everywhere else, the known gaps (concurrent `EnsureUserIdentityAsync` calls, no `RequestFailedException` translation, `Leave`'s still-fragile `RemoveParticipantAsync`) were left exactly as Task 3 left them, per the explicit "build now, harden later" decision. `dotnet build` — 0 errors, no new warnings.
- Applied 7 of Task 3's 8 deferred review patches (user reviewed each individually and approved all but the `.env.example` newline). Two follow-on changes beyond the patches themselves, both required for correctness once `EnsureUserIdentityAsync` stopped self-committing: `StartBoardCallCommand` now resolves the ACS identity *before* its existing `SaveChangesAsync` (one commit covers both the identity change and the new `BoardCall` row); `JoinBoardCallCommand` gained its own explicit `SaveChangesAsync` call, since previously it never committed anything itself. `✅ Resolved review finding [medium]`: all 6 non-idempotency findings (folder placement ×2, input validation, whitespace check, logging). `✅ Resolved review finding [high]`: the `RequestFailedException`/idempotency gap, since it was already causing a reachable bug in `LeaveBoardCallCommand`. `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new.
- Task 5 implemented: `BoardCallEventsController` (Event Grid subscription-validation handshake + `CallParticipantAdded`/`CallParticipantRemoved` dispatch), `RecordCallParticipantJoinedCommand`, `RecordCallParticipantLeftCommand`. Every SDK/wire-format detail was verified against live docs before writing code, not assumed: the `Microsoft.EventGrid.SubscriptionValidationEvent` and `Microsoft.Communication.CallParticipantAdded`/`CallParticipantRemoved` JSON schemas, and the `Azure.Messaging.EventGrid` .NET API surface (`EventGridEvent.ParseMany`, `BinaryData.ToObjectFromJson<T>()`). This caught a route-prefix bug before it shipped: the story's literal `"api/internal/board-call-events"` route would have doubled the `PrefixConventionConfigurator`-injected `"api"` prefix and bypassed `InternalApiKeyMiddleware`'s guard entirely — fixed by matching `InternalBoardsController`'s actual `[Route("internal/boards")]` precedent instead. No SDK type for `SubscriptionValidationResponse` could be confirmed, so a small local record was used instead of guessing its constructor shape. Extended `IBoardCallRepository`/`IUserRepository` with the reverse-lookup methods this task's idempotent join/leave handling needed. Fixed 3 new CS8602 nullable-reference warnings (added explicit null-pattern checks on the deserialized Event Grid payloads) and one `CS0246` compile error (missing `using Azure.Core;` for `AccessToken`) during implementation. `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new.
- Code review of Task 4 & Task 5 ran (Blind Hunter + Edge Case Hunter + Acceptance Auditor, scoped to only these two tasks' files, excluding Task 3's already-patched files): 1 `decision-needed` (all three reviewers independently converged on it), 8 `patch`, 3 `defer`, 6 dismissed. Before rating, every finding's location was read in the actual source (not judged from the diff hunk alone) — this caught that one Blind Hunter finding (missing `[AllowAnonymous]`) was a misread once checked against `InternalBoardsController`'s real precedent (also has neither attribute), so it was dismissed rather than patched.
  - **Decision applied**: participant-capacity enforcement was unreliable because `BoardCallParticipant` rows were only ever written asynchronously by the webhook, so the count `JoinBoardCallCommand` checked against was stale by construction — concurrent/rapid joins could exceed `MaxParticipants`, and a reconnecting participant could be wrongly rejected at cap. Per the user's explicit choice, `StartBoardCallCommand`/`JoinBoardCallCommand` now insert the `BoardCallParticipant` row **synchronously** as a reservation (Join also checks for the caller's own existing active row first, to support reconnects without double-counting them), with the row rolled back (and for Start, the room too) if the subsequent ACS calls fail. The user explicitly declined a new Hangfire cleanup job for orphaned reservations, since this story's Dev Notes §3 already removed that exact grace-period mechanism during the ACS pivot and reintroducing it would reverse a recorded decision — synchronous rollback achieves the same guarantee (no permanently-stuck phantom seats) without new infrastructure.
  - **Follow-on fix required by the decision above (not one of the original 8/9 findings)**: `RecordCallParticipantJoinedCommand` previously used an any-status lookup and could "reactivate" a stale (already-left) participant row. Since Join now creates a fresh row per session, a rejoin can leave multiple historical rows for the same `(BoardCallId, UserId)`; reactivating the wrong one would set two rows' `LeftAt` to `null` at once, violating `BoardCallParticipantConfiguration`'s partial unique index (`(BoardCallId, UserId) WHERE "LeftAt" IS NULL`) — the resulting `ConflictException` would make Event Grid retry the same failing delivery indefinitely. Fixed by switching the lookup to the active-only query; the now-unused `GetParticipantAsync` repository method was removed rather than left as dead scaffolding.
  - **Patches applied**: `BoardCallLifecycleService.EndCallAsync` now injects `IAcsCallService` and deletes the ACS room itself — this was the most consequential fix, since the auto-end path (`EndCallIfEmptyAsync`, triggered whenever the last participant leaves — the ordinary way most calls end) previously never deleted the room at all, leaking it permanently; `EndBoardCallCommand`'s own now-redundant `DeleteRoomAsync` call was removed. The same method also now synchronously closes out every still-open `BoardCallParticipant` row before the room is deleted, so an explicit "end for everyone" no longer leaves other participants' rows permanently `LeftAt = null` once their own webhook events arrive too late to find an "active" call. A new general-purpose unique-violation → `ConflictException` translation was added to `UnitOfWork.SaveChangesAsync` (Postgres `23505`/`PostgresErrorCodes.UniqueViolation`, verified via the Npgsql docs before using it) — this lives in Persistence, not Application, since Application cannot reference `Npgsql`/EF types directly per AD-1; `StartBoardCallCommand` now also cleans up the ACS room if the DB write fails after the room was already created. `BoardCallEventsController` gained malformed-event logging and a `JsonException` catch around `EventGridEvent.ParseMany` (verified as the actual exception type this SDK throws, rather than guessed) returning `BadRequest()` instead of an unhandled 500. Cosmetic: a stray blank line in `BoardAccessService.cs`, and `BoardRolePermissions.CanStartCall` renamed to `CanManageCall` (matching the existing `CanManage*` naming convention) with a new `EnsureCanEndCallAsync` so `EndBoardCallCommand` no longer authorizes via a method literally named "Start".
  - `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new.
- Task 6 implemented: `IBoardCallsApi`, `IBoardCallApiService`/`BoardCallApiService`, `IBoardCallStore`/`BoardCallStore`, wired into `BoardActionsHubService.ConnectAsync` and `Services/DI/ServiceCollectionExtensions.cs`. Two real gaps found and filled while implementing, beyond the literal checklist:
  - **No `BoardCallsController` existed anywhere in the backend.** Task 4 built `StartBoardCallCommand`/`JoinBoardCallCommand`/`LeaveBoardCallCommand`/`EndBoardCallCommand`/`GetActiveBoardCallQuery`, but nothing in the story (Task 4 or Task 6) ever mentioned an HTTP controller to dispatch them — without one, `IBoardCallsApi` would have had no endpoint to call. This wasn't ambiguous or a design choice, just a missing mechanical piece the frontend genuinely needs to function, so it was created directly (mirroring `TasksController`'s exact shape: `[Route("boards/{boardId:guid}/calls")]`, `[Authorize]`, thin MediatR dispatch) rather than raised as a blocking question.
  - `ApiResponseExtensions.HandleResponseAsync<T>()` treats a `null` `Content` on a successful response as a failure — this has never mattered before because no existing endpoint legitimately returns a null body on success, but `GetActiveCallAsync`'s "no active call" case is exactly that. Rather than changing the shared, widely-used extension's behavior (and risking effects on other callers), `BoardCallApiService.GetActiveCallAsync` casts to the non-generic `IApiResponse` overload for the success/error check and returns `response.Content` directly.
  - `IBoardCallStore` grew beyond the story's literal `ActiveCall`/`ApplyAction`-only description to include `LoadActiveCallAsync`/`StartCallAsync`/`JoinCallAsync`/`LeaveCallAsync`/`EndCallAsync` — required by `project-context.md`'s fixed frontend call chain (components inject the Store only, never `*ApiService` or a Refit interface directly), without which Task 8's UI components would have nothing valid to call.
  - `BoardActionSyncKey.cs` gap (deferred here from the Task 1/2 review) closed: `CallStarted`/`CallParticipantsChanged`/`CallEnded` now resolve to `call:{BoardCallId}` instead of falling through to the generic `type:{byte}` key.
  - `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new.
- Task 7 implemented: `package.json`/`package-lock.json`, `js-src/acsCallInterop.js`, `AcsCallInteropService`/`IAcsCallInteropService`, updated `Dockerfile`, `.gitignore`, and `README.md`. Every ACS Calling SDK API call was verified against the live Microsoft Learn `Call` interface reference and the official Rooms quickstart sample before writing it, not assumed — this caught a real error in the story's own checklist text: it named the screen-sharing methods `startScreenSharingAsync()`/`stopScreenSharingAsync()`, but the current GA Web SDK's actual methods are `call.startScreenSharing()`/`call.stopScreenSharing()` (no `Async` suffix). Also caught that `@azure/communication-common` (for `AzureCommunicationTokenCredential`) is a separate required package the story's dependency list omitted. `deviceManager.askDevicePermission({audio, video})` was confirmed to resolve to `{audio, video}` booleans rather than throw on denial, which is what makes AC4's audio-only/view-only join possible without special-case error handling. One deliberate architectural deviation: `AcsCallInteropService` orchestrates `IBoardCallStore` (Task 6) instead of `IBoardCallApiService` directly as the checklist literally said, to avoid a duplicate Start/Join network call between this service and whatever Task 8's UI calls on the Store. Verified beyond just `dotnet build`: ran `npm install`/`npm run build` locally (produced a working 7.3 MB bundle) and a full `docker build` against the updated Dockerfile end-to-end, confirming the whole pipeline actually works rather than just looking correct on paper. `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new.
- Code review of Task 6 & Task 7 (frontend) ran (Blind Hunter + Edge Case Hunter + Acceptance Auditor, scoped to `src/Frontend/`/`README.md`/`.gitignore`, `package-lock.json` excluded as generated/not worth reviewing): 0 `decision-needed`, 13 `patch`, 6 `defer`, 4 dismissed. All 13 patches applied per explicit user instruction.
  - The most consequential fix, converged on independently by all three reviewers: `AcsCallInteropService.StartCallAsync`/`JoinCallAsync` had no compensating cleanup if the ACS session failed to connect after the backend reservation already committed — exactly the class of bug the backend review already caught server-side. Both now wrap the ACS-joining step in try/catch and release the reservation (`EndCallAsync`/`LeaveCallAsync`) on failure before rethrowing.
  - A real AC4 violation was caught and fixed: `joinRoom`'s returned `videoAvailable` didn't check whether a camera device actually existed, only whether permission was granted — now correctly `access.video && !!videoOptions`.
  - A genuine dedup-correctness bug already shipped in Task 6 was caught and fixed: `BoardActionSyncKey` gave `CallStarted`/`CallParticipantsChanged`/`CallEnded` the same bare key, which could let a delayed Event-Grid-driven notification cause the sync guard to silently drop a legitimate, differently-typed one for the same call. Each type now has its own sub-key, matching the existing sub-key convention used for Task's position/comments-count/attachments-count variants.
  - Re-entrancy protection (`IsInCall` guard + JS-side defensive `callAgent`/`call` cleanup), error handling around the JS `leaveCall()` call so a messy hangup can't strand local or backend state, `DisposeAsync` now hanging up an active call, `askDevicePermission` no longer able to abort the whole join on a throw, `attachRenderer` no longer leaving stale DOM nodes on container reuse, and new mid-call video streams (e.g. screen share) now getting their own availability-change subscription.
  - `BoardCallStore.LoadActiveCallAsync` no longer leaves `ActiveCall` pointing at a stale board's data if the load fails after `BoardId` was already reassigned.
  - Lower-severity mechanical fixes: `--minify` added to the esbuild script (verified: 7.3 MB → 5.3 MB), `CancellationToken` usage made consistent across `IAcsCallInteropService` and actually forwarded into the JS interop calls, and the Dockerfile's `npm ci` moved before `COPY . .` for a real caching benefit (verified via two `docker build` runs — the second showed the `npm ci` layer `CACHED`) while `npm run build` correctly stays after `COPY . .`, since it needs the copied `js-src/` source.
  - `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new.
- Task 8 implemented (story complete): `BoardCallIndicator.razor`, `BoardCallPanel.razor`, wired into `Pages/Boards/Board.razor`. Two necessary cross-cutting additions surfaced only once both components existed together and needed to react to each other's state:
  - `IAcsCallInteropService` gained a general `StateChanged` event (previously only had `RemoteParticipantsChanged`) — without it, `BoardCallPanel` had no way to learn that `BoardCallIndicator`'s Start/Join button had just succeeded, since both components share the same app-lifetime-scoped instance under Blazor WASM's single-scope DI model but have no other channel to coordinate through.
  - `acsCallInterop.js` gained a `call.on("stateChanged")` listener invoking a new `[JSInvokable] OnCallDisconnected()` when ACS itself disconnects the call (e.g. a different ScrumMaster/Admin ending it for everyone) — without this, AC6 ("no one can silently continue the call afterward") would not actually hold for a client that never explicitly clicked Leave: `IsInCall` would stay stuck `true` and `BoardCallPanel` would keep rendering a dead session. `OnCallDisconnected` only resets local flags — it deliberately does not call the backend Leave/EndCallAsync again, since whoever actually ended the call already made that call.
  - Corrected this task's own earlier-written Task 8 checklist text (written before implementation, during the "match correct patterns" pass after Task 7's review): participant tiles use `RemoteParticipantInfo.DisplayName` directly rather than cross-referencing `IBoardCallStore.ActiveCall.Participants` for `DisplayName`/`AvatarUrl` as originally planned — `RemoteParticipantInfo.Id` (ACS `communicationUserId`) and `BoardCallParticipantDto.UserId` (our internal `Guid`) don't share a common key, so that cross-reference was never actually implementable. Not a real gap: the ACS SDK's own `displayName` (set by each participant's own client from their `profileStore.Profile?.DisplayName` at join time) already provides a usable name.
  - `BoardCallIndicator`'s `MaxParticipants = 4` is a local frontend constant mirroring the backend's `BoardCallConstants.MaxParticipants` — no DTO currently surfaces this value to the client; a known, accepted small duplication rather than new scope to add one.
  - `Board.razor`'s `Dispose()` now calls `CallInterop.LeaveCallAsync()` (fire-and-forget) if still in a call when navigating away, mirroring the existing `ExportHubService.DisconnectAsync()` pattern — otherwise a call would keep running in the background indefinitely after leaving the board page, since `IAcsCallInteropService` has no other lifecycle hook to catch this in Blazor WASM's single-scope model.
  - **Verification limitation, stated plainly**: `dotnet build -t:Rebuild` (0 errors, 17 pre-existing warnings, none new) and `npm run build` both verified. **No live in-browser test of the actual call flow was possible** — this requires a configured Azure AD tenant and a live Azure Communication Services resource, neither available in this environment. Per `project-context.md`'s testing rule (no test project exists), build success is the verification gate used throughout this story, but a UI feature of this kind ordinarily warrants a real browser check that could not be performed here.
- Full story code review completed and all patches applied, triggered by manual testing (proof that build-only verification has real limits for UI features — exactly the gap flagged in Task 8's own completion notes above). All three reviewers independently root-caused both reported bugs to the same underlying issues:
  - Camera-off: a DOM ownership conflict (JS interop's `container.replaceChildren()` deleting Blazor-owned sibling nodes inside the same tile `<div>`) compounded by a missing renderer-disposal call when toggling video off. Fixed by giving each tile a dedicated, JS-exclusive `.call-tile-video` child div (Blazor never renders into it) and adding proper attach/detach tracking in `BoardCallPanel.OnAfterRenderAsync`, closing the identical latent bug for remote participants losing video too (not reported, but same root cause).
  - Hover CSS: `.btn:hover` and `.btn-primary:hover` set different properties at equal specificity, so both applied — the primary button's hover showed the wrong (neutral gray) background dimmed by the intended filter. Fixed by excluding `.btn-primary` from the generic hover rule and giving it its own explicit hover background.
  - Embedded panel vs. separate page: confirmed the spec never actually decided this. Per your explicit choice, built `BoardCall.razor` at a new route (`/boards/{BoardId:guid}/call`) that owns the entire call lifecycle (Start/Join on mount, Leave on dispose) — this also fixes a concrete bug Blind Hunter found independently: `Board.razor`'s `Dispose()` (which previously called `LeaveCallAsync` as a safety net) never actually fires on a same-route board switch, only on `OnParametersSetAsync`, so a user switching boards mid-call kept their session connected to the *previous* board while the still-rendered panel began acting against the *new* board's `BoardId`. `Board.razor` no longer injects `IAcsCallInteropService` or renders `BoardCallPanel` at all — the dedicated call page owns that now.
  - Backend: `LeaveBoardCallCommand` now synchronously marks `LeftAt` (closing a capacity-blocking asymmetry with Start/Join's already-synchronous reservation) and `BoardCallEventsController` now isolates per-event JSON parse failures so one malformed event can't 500 an entire Event Grid batch.
  - `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new; `npm run build` re-verified after the `detachRenderer` addition.
- Manual-testing bug reports + full `deferred-work.md` sweep (this session), addressed as two batches:
  - **User-reported bugs, fixed**: (1) error toasts showing a raw JS stack trace — `AcsCallInteropService` now centralizes every JS call through `InvokeVoidSafeAsync`/`InvokeSafeAsync<T>`, which catch `JSException` and rethrow with only the first line of `.Message` (the rest is stack frames, an artifact of Blazor surfacing the JS `Error.stack` as the whole exception message); (2) no pre-join camera/mic preview — new `BoardCallPreview.razor` + JS `initPreview`/`setPreviewCamera`/`disposePreview`, shown as a new first step in `BoardCall.razor`'s state machine before any backend Start/Join call happens, so the camera can be previewed and toggled without yet reserving a call seat; (3) mic/camera toggle always showing "on" regardless of the real state — new `IsMicOn`/`IsCameraOn` state on `IAcsCallInteropService`, set from the preview's actual chosen flags (threaded through as new `micEnabled`/`cameraEnabled` params on `StartCallAsync`/`JoinCallAsync`/JS's `joinRoom`, which now mutes/omits video accordingly instead of always defaulting on), and `BoardCallPanel` now reads these directly instead of shadowing them in local fields seeded from device *availability*; (4) participant count not decreasing for the leaver — `BoardCallStore.LeaveCallAsync` now optimistically removes the current user from `ActiveCall.Participants` locally, since `BoardActionsSyncGuard` deliberately filters a user's own notifications back to them; (5) no redirect after Leave/End — `BoardCall.razor` now subscribes to `StateChanged` and navigates back to the board on any true→false `IsInCall` transition, covering both an explicit leave and a remote "ended for everyone"; (6) local tile showing literal "You" — now shows `{DisplayName} (You)` via `IProfileStore`, falling back to "You" only if no display name exists.
  - **Deferred-work sweep**: worked through all 14 open items in `deferred-work.md`. Notable fixes: a shared `user:{userId}:acs-identity` distributed lock (via the existing `IUnitOfWork.AcquireDistributedLockAsync`) now closes the concurrent-identity-provisioning race in both `StartBoardCallCommand` and `JoinBoardCallCommand`; `JoinBoardCallCommand`'s capacity-check-then-reserve sequence is now wrapped in a `boardcall:{callId}:participants`-locked transaction, mirroring the exact pattern `CreateBoardCommand`/`CreateTaskCommand` already use for the same class of race; `StartBoardCallCommand`/`JoinBoardCallCommand`'s rollback `catch` blocks no longer risk masking the original exception if the cleanup itself throws (wrapped in their own try/catch, logged, never rethrown); `RecordCallParticipantJoinedCommand`/`RecordCallParticipantLeftCommand` now guard against out-of-order Event Grid delivery via a new `IBoardCallRepository.GetLatestParticipantAsync`; three handlers (`RecordCallParticipantJoinedCommand`/`LeftCommand`, `LeaveBoardCallCommand`) no longer send a `CallParticipantsChanged` notification with an empty participant list when the call ended concurrently; `BoardCallStore` gained a generation counter so a slow `LoadActiveCallAsync` GET can never clobber fresher state from a real-time notification or a subsequent Start/Join/Leave/End call; `getParticipantId` in `acsCallInterop.js` now falls back to a `WeakMap`-backed unique id instead of a collision-prone empty string; `DeleteBoardCommand`/`DeleteWorkspaceCommand` now end any active board call(s) before soft-deleting (new `IBoardCallRepository.GetActiveCallsForWorkspaceAsync` for the workspace-wide case); `CreateRoomAsync` now sets a 24h safety-net `validUntil` instead of `null` (explicitly not a real call-duration policy, just an abandoned-room upper bound). Two items were left **consciously unresolved** with the reasoning recorded directly in `deferred-work.md`: the speculative cross-board single-active-call constraint (no AC requires it — would be scope creep) and the Dockerfile's unpinned Node.js install (it's the exact snippet the story itself prescribed for Task 7). Three low-value/infra-gap items were left exactly as previously documented (no ACS emulator exists; the shared static internal API key is a pre-existing cross-cutting architecture decision; the bundle-filename magic string and remote-participant `.off()` cleanup are low-value nits).
  - `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new (verified after each of the two batches above); `npm run build` re-verified twice (once after the preview-screen JS changes, once after the `getParticipantId` hardening).
  - **Verification limitation, unchanged from every prior round**: no live in-browser test of the call flow was possible — this environment has no configured Azure AD tenant or live Azure Communication Services resource. All verification here is build-level (compiles cleanly, JS bundles cleanly) plus manual code tracing against the exact bug reports, not a confirmed working end-to-end call.
- Immediate follow-up bug reports from the previous round (this session), all fixed:
  - **"End call for everyone" appeared broken (instant redirect, "Board is not loaded" toast, call kept running)**: two compounding races. First, `AcsCallInteropService.EndCallAsync`/`LeaveCallAsync` reset local state and fired `StateChanged` (triggering the previous round's auto-navigate-on-disconnect fix) *before* the backend Leave/End HTTP call even went out — reordered so the backend call always completes first. Second — the actual source of the exact error text — even correctly ordered, navigating away triggers `Board.razor.OnParametersSetAsync` to call `BoardCallStore.Reset()` concurrently with the still-in-flight request reading the same store's ambient `BoardId`; this is a known category of Blazor gotcha (component disposal on navigation isn't guaranteed to complete before the next page's lifecycle runs), so rather than trying to sequence around it, `IBoardCallStore`'s and `IAcsCallInteropService`'s `StartCallAsync`/`JoinCallAsync`/`LeaveCallAsync`/`EndCallAsync` now all take an explicit `Guid boardId` parameter from the caller (always known unambiguously — `BoardCall.razor`'s own route parameter, now also passed to a new `BoardCallPanel.BoardId` parameter) instead of trusting the shared, concurrently-mutable store property; `BoardCallStore`'s mutations now additionally guard with `if (BoardId == boardId)` so a response resolving after the store moved on to a different board is silently dropped, never misapplied. This also fixes the separately-reported "Board is not loaded" when simply leaving the board page mid-call, since it's the identical race.
  - **Call panel too tall / no scroll with one participant**: `.call-tiles`' grid previously let a single tile stretch to the full container width (`minmax(180px, 1fr)`), and the 16:9 aspect ratio then made it taller than the viewport with no way to scroll. Fixed by capping tile width (`minmax(200px, 360px)` + `justify-content: center`) so a lone tile no longer stretches, and making `.call-panel` a bounded flex column whose `.call-tiles` region (`flex: 1 1 auto; min-height: 0; overflow-y: auto`) scrolls independently if content still overflows on a short viewport, while the header and control bar stay fixed in place.
  - **Call controls moved to bottom-center**: mic/camera/screen-share/leave/end-for-everyone moved out of the top-right `.call-panel-head` into a new `.call-controls` bar centered below the video tiles.
  - `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new. No JS changes this round, so `npm run build` wasn't re-run.
  - **Verification limitation, unchanged**: no live in-browser test was possible in this environment; verification is build-level plus manual tracing of the exact reported symptoms against the fixed code paths.
- Call capacity reworked from a fixed global constant to the board's own membership (this session): removed `Domain.Constants.BoardCallConstants.MaxParticipants` (a flat `4`, unrelated to either the board or the workspace, despite reading that way to you) and replaced it with a live count of the board's own members. Added `IBoardMemberRepository.CountByBoardIdAsync`/`BoardMemberRepository` implementation; added `BoardCallDto.MaxParticipants`, populated from that count everywhere a `BoardCallDto` is produced (`StartBoardCallCommand`, `JoinBoardCallCommand`, `GetActiveBoardCallQuery`); `JoinBoardCallCommand`'s capacity check now compares against this board-member count instead of the removed constant. `BoardCallMappings.ToDto` gained a required `maxParticipants` parameter; the three call sites that only ever needed the participant list (`RecordCallParticipantJoinedCommand`/`LeftCommand`, `LeaveBoardCallCommand`) were switched to a new `ToParticipantDtos` helper rather than passing a throwaway placeholder value just to satisfy the new parameter. Frontend: `BoardCallIndicator.razor`'s own local `MaxParticipants = 4` (previously an explicitly-accepted duplication of the backend constant) is gone — it reads `activeCall.MaxParticipants` directly off the same shared `Contracts.DTOs.BoardCallDto` the backend populates, so there's one source of truth instead of two numbers that could drift.
  - `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new. No JS changed, so `npm run build` wasn't re-run.
  - **Verification limitation, unchanged**: no live in-browser test was possible in this environment.
- Five bugs from continued manual testing, all fixed (this session):
  - **Unhandled crash rendering a video tile** ("Failed to render video stream, this stream is not longer available..."): `attachRenderer`'s `renderer.createView()` can reject in the race window between an availability check and the render resolving (the participant turns their camera/share off in between); it wasn't caught, so the rejection surfaced as an unhandled exception in `BoardCallPanel.OnAfterRenderAsync` — a Blazor lifecycle method with no error boundary — crashing the whole WASM renderer. Fixed at both ends: `attachRenderer` now catches `createView()` failures and returns `false`; `SyncRendererAsync`'s attach/detach calls are now wrapped defensively too, so no future interop surprise can crash the app this way.
  - **Screen share couldn't be restarted after stopping once**: `BoardCallPanel`'s local `_screenSharing` bool only changed on our own button clicks, so stopping sharing via the browser's native "Stop sharing" bar left it stuck `true` — every further click tried to *stop* an already-stopped share (fails) and could never *start* a new one. Fixed by adding a real, event-driven `IsScreenSharing` to `IAcsCallInteropService`, sourced only from a new `call.on("isScreenSharingOnChanged", ...)` listener (→ `[JSInvokable] OnScreenSharingChanged`) — the same "single event-driven source of truth" pattern already used for `IsMicOn`/`IsCameraOn`, never set optimistically by the Start/Stop methods themselves.
  - **Screen share was never actually rendered for anyone**: the feature had only ever wired the start/stop toggle — no code anywhere attached a renderer to a screen-share stream, local or remote. `mediaStreamType` ("Video" vs "ScreenSharing") is now used to distinguish a participant's camera from their screen share (previously any available stream was picked arbitrarily); `RemoteParticipantInfo` gained `IsScreenSharing`; new full-width `.call-tile-screen` tiles render above the regular grid for whoever is currently sharing (local and/or remote), using `object-fit: contain` instead of `cover` so shared content isn't cropped.
  - **Call tiles too small, not using available space**: the previous round's `minmax(200px, 360px)` width cap (added to stop a lone tile from being absurdly tall) also stopped tiles from growing with 2+ participants. Switched to `minmax(280px, 1fr)` and moved the size guard from width to `max-height: 46vh` on `.call-tile`, so tiles now use available space while still never overflowing the viewport unscrollably.
  - **Avatars never shown; fallback color inconsistent with the rest of the app**: placeholders always showed flat-colored initials, ignoring `AvatarUrl` and this app's existing deterministic `AvatarColorHelper` (used everywhere else — sidebar, task assignees, etc.). Fixed for the local tile via `IProfileStore`; for remote tiles, added `BoardCallParticipantDto.AcsCommunicationUserId` (from `User.AcsCommunicationUserId`) so `BoardCallPanel` can correlate `RemoteParticipantInfo.Id` (the ACS SDK's only participant identifier) back to `BoardCallStore.ActiveCall.Participants`' `AvatarUrl` — closing the "no shared key between these two participant lists" gap flagged as a known limitation in earlier rounds.
  - Every ACS SDK behavior relied on here (`mediaStreamType`, `isScreenSharingOnChanged`, `createView()` rejecting for a since-unavailable stream) was verified against the installed `@azure/communication-calling` package's own `.d.ts` type definitions before writing the code, not assumed.
  - `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new; `npm run build` re-verified (substantial `acsCallInterop.js` changes this round).
  - **Verification limitation, unchanged**: no live in-browser test was possible in this environment.
- Two more bugs, one a direct regression from the previous round (this session):
  - **Own camera/screen video invisible to yourself**: the previous round's crash fix added `if (!stream || !stream.isAvailable) return false;` to `attachRenderer` — but `isAvailable` only exists on `RemoteVideoStream`, confirmed against the SDK's `.d.ts`; `LocalVideoStream` has no such field, so it's always `undefined` → falsy, silently blocking every local render (camera and screen-share tiles) for everyone, 100% of the time. Fixed by checking only `!stream` — remote streams are already filtered by `isAvailable` inside `findRemoteVideoStream` itself, so the extra check on the resolved stream was redundant there and actively broken for local.
  - **"End call for everyone" didn't force other participants out**: ending a call deletes the ACS Room, but Room deletion isn't a documented guarantee of forcibly disconnecting a participant's *already-established* live media session (Rooms mainly gate join eligibility) — other clients could just keep running indefinitely. Added `IBoardCallStore.CallEndedRemotely`, fired only when an incoming `CallEnded` notification matches the client's own tracked `ActiveCall` (which — thanks to `BoardActionsSyncGuard` already filtering the actor's own notification — only ever reaches other participants, never the ender). `AcsCallInteropService` subscribes and force-hangs-up its local ACS session the moment it fires, independent of whatever ACS itself does with the deleted room. Required converting `AcsCallInteropService` from a primary-constructor class to a regular one (fields renamed `_jsRuntime`/`_boardCallStore`/`_profileStore`) since subscribing an event handler at construction needs a real constructor body.
  - `dotnet build -t:Rebuild` — 0 errors, 17 pre-existing warnings, none new; `npm run build` re-verified after the `attachRenderer` fix.
  - **Verification limitation, unchanged**: no live in-browser test was possible in this environment.

### File List

**Modified (local-stream regression fix + force-leave-on-end-for-everyone, this session):**
- `src/Frontend/WebApp/js-src/acsCallInterop.js` (`attachRenderer` no longer checks `isAvailable` on the resolved stream)
- `src/Frontend/Services.Abstractions/BoardCalls/IBoardCallStore.cs` (added `CallEndedRemotely` event)
- `src/Frontend/Services/BoardCalls/Stores/BoardCallStore.cs` (fires `CallEndedRemotely` from `ApplyCallEnded`)
- `src/Frontend/Services/BoardCalls/AcsCallInteropService.cs` (converted to a regular constructor; subscribes to `CallEndedRemotely` and force-hangs-up locally)

**Modified (crash fix, screen-share desync + rendering, tile sizing, real avatars, this session):**
- `src/Frontend/WebApp/js-src/acsCallInterop.js` (mediaStreamType-aware stream resolution for camera vs screen-share; `isScreenSharingOnChanged` listener; `createView()`/`renderer.dispose()` wrapped defensively)
- `src/Frontend/Services.Abstractions/BoardCalls/RemoteParticipantInfo.cs` (added `IsScreenSharing`)
- `src/Frontend/Services.Abstractions/BoardCalls/IAcsCallInteropService.cs` (added `IsScreenSharing`)
- `src/Frontend/Services/BoardCalls/AcsCallInteropService.cs` (added `IsScreenSharing`, reset alongside other call state; new `OnScreenSharingChanged` JSInvokable)
- `src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor` (screen-share tiles; binds to `CallInterop.IsScreenSharing`; defensive try/catch around renderer attach/detach; real avatar images + `AvatarColorHelper`; injects `IBoardCallStore`)
- `src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor.css` (tile sizing switched from width-cap to `max-height`; new `.call-tile-screen` styles; avatar image styles)
- `src/Shared/Contracts/DTOs/BoardCalls/BoardCallParticipantDto.cs` (added `AcsCommunicationUserId`)
- `src/Backend/Application/Common/Mappings/BoardCallMappings.cs` (populates `AcsCommunicationUserId`)

**Modified (call capacity reworked to board membership, this session):**
- `src/Shared/Contracts/DTOs/BoardCalls/BoardCallDto.cs` (added `MaxParticipants`)
- `src/Backend/Application/Common/Mappings/BoardCallMappings.cs` (`ToDto` now takes `maxParticipants`; added `ToParticipantDtos`)
- `src/Backend/Application/Interfaces/Repositories/IBoardMemberRepository.cs` (added `CountByBoardIdAsync`)
- `src/Backend/Persistence/Repositories/BoardMemberRepository.cs` (implemented the above)
- `src/Backend/Application/Features/BoardCalls/Commands/JoinBoardCallCommand.cs` (capacity check now uses board-member count instead of the removed constant)
- `src/Backend/Application/Features/BoardCalls/Commands/StartBoardCallCommand.cs` (populates `MaxParticipants` on the returned DTO)
- `src/Backend/Application/Features/BoardCalls/Queries/GetActiveBoardCallQuery.cs` (populates `MaxParticipants` on the returned DTO)
- `src/Backend/Application/Features/BoardCalls/Commands/RecordCallParticipantJoinedCommand.cs` (switched to `ToParticipantDtos`)
- `src/Backend/Application/Features/BoardCalls/Commands/RecordCallParticipantLeftCommand.cs` (switched to `ToParticipantDtos`)
- `src/Backend/Application/Features/BoardCalls/Commands/LeaveBoardCallCommand.cs` (switched to `ToParticipantDtos`)
- `src/Backend/Domain/Constants/BoardCallConstants.cs` (removed `MaxParticipants`)
- `src/Frontend/WebApp/Components/BoardCallIndicator/BoardCallIndicator.razor` (removed the local `MaxParticipants` constant; reads `activeCall.MaxParticipants`)

**Modified (End-call race + call-panel layout fixes, this session):**
- `src/Frontend/Services.Abstractions/BoardCalls/IBoardCallStore.cs` (`StartCallAsync`/`JoinCallAsync`/`LeaveCallAsync`/`EndCallAsync` now take an explicit `Guid boardId`)
- `src/Frontend/Services/BoardCalls/Stores/BoardCallStore.cs` (implements the above; state mutations guarded with `BoardId == boardId`; dropped the now-unreachable "Board is not loaded" throws)
- `src/Frontend/Services.Abstractions/BoardCalls/IAcsCallInteropService.cs` (`StartCallAsync`/`JoinCallAsync`/`LeaveCallAsync`/`EndCallAsync` now take an explicit `Guid boardId`)
- `src/Frontend/Services/BoardCalls/AcsCallInteropService.cs` (implements the above; `LeaveCallAsync`/`EndCallAsync` now call the backend before tearing down local state)
- `src/Frontend/WebApp/Pages/Boards/BoardCall.razor` (passes its own `BoardId` route parameter through to every call; passes `BoardId` to `BoardCallPanel`)
- `src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor` (added `[Parameter] BoardId`; controls moved into a new bottom `.call-controls` bar)
- `src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor.css` (`.call-panel`/`.call-tiles` bounded-height + scroll fix; capped tile width; new `.call-controls` styles)

**Added (manual-testing bug-fix + deferred-work sweep, this session):**
- `src/Frontend/Services.Abstractions/BoardCalls/PreviewDeviceAccess.cs`
- `src/Frontend/WebApp/Components/BoardCallPreview/BoardCallPreview.razor`
- `src/Frontend/WebApp/Components/BoardCallPreview/BoardCallPreview.razor.css`

**Modified (manual-testing bug-fix + deferred-work sweep, this session):**
- `src/Frontend/Services.Abstractions/BoardCalls/IAcsCallInteropService.cs` (added `IsMicOn`/`IsCameraOn`, `StartPreviewAsync`/`SetPreviewCameraAsync`/`StopPreviewAsync`; `StartCallAsync`/`JoinCallAsync` gained `micEnabled`/`cameraEnabled` params)
- `src/Frontend/Services/BoardCalls/AcsCallInteropService.cs` (JS-error-message cleanup wrappers, preview methods, `IsMicOn`/`IsCameraOn` tracking)
- `src/Frontend/WebApp/js-src/acsCallInterop.js` (`initPreview`/`setPreviewCamera`/`disposePreview`; `initCallAgent`/`joinRoom` reuse preview state and honor `micEnabled`/`cameraEnabled`; hardened `getParticipantId` fallback)
- `src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor` (binds directly to `CallInterop.IsMicOn`/`IsCameraOn`; local tile shows `{DisplayName} (You)`)
- `src/Frontend/WebApp/Pages/Boards/BoardCall.razor` (new preview step in the state machine; auto-redirect to the board on any true→false `IsInCall` transition)
- `src/Frontend/Services/BoardCalls/Stores/BoardCallStore.cs` (optimistic local participant removal in `LeaveCallAsync`; generation counter guarding `LoadActiveCallAsync` against a concurrent notification or Start/Join/Leave/End)
- `src/Backend/Application/Features/BoardCalls/Commands/StartBoardCallCommand.cs` (per-user ACS-identity distributed lock; rollback cleanup can no longer mask the original exception)
- `src/Backend/Application/Features/BoardCalls/Commands/JoinBoardCallCommand.cs` (per-call participant-capacity distributed lock closing the residual TOCTOU race; per-user ACS-identity distributed lock; rollback cleanup can no longer mask the original exception)
- `src/Backend/Application/Features/BoardCalls/Commands/LeaveBoardCallCommand.cs` (skips the `CallParticipantsChanged` notification if the call ended concurrently)
- `src/Backend/Application/Features/BoardCalls/Commands/RecordCallParticipantJoinedCommand.cs` (out-of-order Event Grid delivery guard via `GetLatestParticipantAsync`; skips the notification if the call ended concurrently)
- `src/Backend/Application/Features/BoardCalls/Commands/RecordCallParticipantLeftCommand.cs` (out-of-order Event Grid delivery guard; skips the notification if the call ended concurrently)
- `src/Backend/Application/Features/Boards/Commands/DeleteBoardCommand.cs` (ends the board's active call, if any, before soft-deleting)
- `src/Backend/Application/Features/Workspaces/Commands/DeleteWorkspaceCommand.cs` (ends every active call across the workspace's boards before soft-deleting)
- `src/Backend/Application/Interfaces/Repositories/IBoardCallRepository.cs` (added `GetLatestParticipantAsync`, `GetActiveCallsForWorkspaceAsync`)
- `src/Backend/Persistence/Repositories/BoardCallRepository.cs` (implemented the above)
- `src/Backend/Infrastructure/Boards/Calls/AcsCallService.cs` (`CreateRoomAsync` now sets a 24h safety-net `validUntil` instead of `null`)
- `_bmad-output/implementation-artifacts/deferred-work.md` (per-item resolution status for this round)

**Modified (Task 4 & Task 5 code review fixes, this session):**
- `src/Backend/Application/Services/BoardCallLifecycleService.cs` (injects `IAcsCallService`, deletes the ACS room, closes out open participant rows on end)
- `src/Backend/Application/Features/BoardCalls/Commands/EndBoardCallCommand.cs` (removed redundant `DeleteRoomAsync` call, switched to `EnsureCanEndCallAsync`)
- `src/Backend/Application/Features/BoardCalls/Commands/StartBoardCallCommand.cs` (synchronous participant reservation + compensating rollback on failure)
- `src/Backend/Application/Features/BoardCalls/Commands/JoinBoardCallCommand.cs` (synchronous participant reservation, reconnect handling, compensating rollback)
- `src/Backend/Application/Features/BoardCalls/Commands/RecordCallParticipantJoinedCommand.cs` (switched to active-only lookup, no longer reactivates stale rows)
- `src/Backend/Persistence/Repositories/UnitOfWork.cs` (unique-violation → `ConflictException` translation)
- `src/Backend/Application/Interfaces/Repositories/IBoardCallRepository.cs` (added `DeleteParticipant`, removed unused `GetParticipantAsync`)
- `src/Backend/Persistence/Repositories/BoardCallRepository.cs` (implemented `DeleteParticipant`, removed `GetParticipantAsync`)
- `src/Backend/Presentation/Controllers/BoardCallEventsController.cs` (malformed-event logging, `JsonException` handling around `ParseMany`)
- `src/Backend/Domain/Authorization/BoardRolePermissions.cs` (`CanStartCall` → `CanManageCall`)
- `src/Backend/Application/Interfaces/Services/IBoardAccessService.cs` (added `EnsureCanEndCallAsync`)
- `src/Backend/Application/Services/BoardAccessService.cs` (implemented `EnsureCanEndCallAsync`, fixed stray blank line)

**Added (Task 6, this session):**
- `src/Backend/Presentation/Controllers/BoardCallsController.cs` (necessary prerequisite — no controller existed to dispatch Task 4's commands/query)
- `src/Frontend/Services/Api/IBoardCallsApi.cs`
- `src/Frontend/Services.Abstractions/BoardCalls/IBoardCallApiService.cs`
- `src/Frontend/Services.Abstractions/BoardCalls/IBoardCallStore.cs`
- `src/Frontend/Services/BoardCalls/BoardCallApiService.cs`
- `src/Frontend/Services/BoardCalls/Stores/BoardCallStore.cs`

**Modified (Task 6, this session):**
- `src/Frontend/Services/Boards/BoardActionSyncKey.cs` (added `call:{BoardCallId}` keys for the 3 call notification types)
- `src/Frontend/Services/Hubs/BoardActionsHubService.cs` (added `IBoardCallStore` dependency + third `.ApplyAction(...)` dispatch)
- `src/Frontend/Services/DI/ServiceCollectionExtensions.cs` (registered `IBoardCallsApi`, `IBoardCallApiService`, `IBoardCallStore`)

**Added (Task 7, this session):**
- `src/Frontend/WebApp/package.json`
- `src/Frontend/WebApp/package-lock.json`
- `src/Frontend/WebApp/js-src/acsCallInterop.js`
- `src/Frontend/Services.Abstractions/BoardCalls/IAcsCallInteropService.cs`
- `src/Frontend/Services.Abstractions/BoardCalls/RemoteParticipantInfo.cs`
- `src/Frontend/Services/BoardCalls/AcsCallInteropService.cs`

**Modified (Task 7, this session):**
- `src/Frontend/WebApp/Dockerfile` (Node.js install + `npm ci && npm run build` before `dotnet publish`)
- `.gitignore` (added `src/Frontend/WebApp/wwwroot/js/*.bundle.js`)
- `README.md` (documented the local `npm install && npm run build` step)
- `src/Frontend/Services/DI/ServiceCollectionExtensions.cs` (registered `IAcsCallInteropService`)

**Added (Task 8, this session):**
- `src/Frontend/WebApp/Components/BoardCallIndicator/BoardCallIndicator.razor`
- `src/Frontend/WebApp/Components/BoardCallIndicator/BoardCallIndicator.razor.css`
- `src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor`
- `src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor.css`

**Modified (Task 8, this session):**
- `src/Frontend/WebApp/Pages/Boards/Board.razor` (wired `IBoardCallStore` reset/load, rendered the two new components, `Dispose()` now leaves an active call on navigation away)
- `src/Frontend/Services.Abstractions/BoardCalls/IAcsCallInteropService.cs` (added `StateChanged` event)
- `src/Frontend/Services/BoardCalls/AcsCallInteropService.cs` (raises `StateChanged`; added `OnCallDisconnected` JS-invokable callback)
- `src/Frontend/WebApp/js-src/acsCallInterop.js` (added `call.on("stateChanged")` remote-disconnect detection)

**Added (full story code review, this session):**
- `src/Frontend/WebApp/Pages/Boards/BoardCall.razor` (new dedicated call page/route, `/boards/{BoardId:guid}/call`)
- `src/Frontend/WebApp/Pages/Boards/BoardCall.razor.css`

**Modified (full story code review, this session):**
- `src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor` (dedicated `.call-tile-video` div per tile; attach/detach lifecycle tracking in `OnAfterRenderAsync`)
- `src/Frontend/WebApp/Components/BoardCallPanel/BoardCallPanel.razor.css` (`.call-tile-video` positioning)
- `src/Frontend/WebApp/js-src/acsCallInterop.js` (added `detachRenderer` export)
- `src/Frontend/Services.Abstractions/BoardCalls/IAcsCallInteropService.cs` (added `DetachRendererAsync`)
- `src/Frontend/Services/BoardCalls/AcsCallInteropService.cs` (implemented `DetachRendererAsync`; fixed `GetModuleAsync` lazy-import race)
- `src/Frontend/WebApp/Components/BoardCallIndicator/BoardCallIndicator.razor.css` (fixed hover CSS specificity collision)
- `src/Frontend/WebApp/Components/BoardCallIndicator/BoardCallIndicator.razor` (Start/Join now navigate to the call page instead of calling `IAcsCallInteropService` directly; removed now-unneeded `_isConnecting`/try-catch)
- `src/Frontend/WebApp/Pages/Boards/Board.razor` (removed inline `BoardCallPanel` render and `IAcsCallInteropService` injection/safety-net Dispose call — the call page now owns this exclusively)
- `src/Backend/Application/Features/BoardCalls/Commands/LeaveBoardCallCommand.cs` (synchronous `LeftAt` + notification + `EndCallIfEmptyAsync`)
- `src/Backend/Presentation/Controllers/BoardCallEventsController.cs` (per-event JSON deserialization exception isolation)

**Added (Task 5, this session):**
- `src/Backend/Presentation/Controllers/BoardCallEventsController.cs`
- `src/Backend/Application/Features/BoardCalls/Commands/RecordCallParticipantJoinedCommand.cs`
- `src/Backend/Application/Features/BoardCalls/Commands/RecordCallParticipantLeftCommand.cs`

**Modified (Task 5, this session):**
- `src/Backend/Presentation/Presentation.csproj` (added `Azure.Messaging.EventGrid` 5.0.0)
- `src/Backend/Application/Interfaces/Repositories/IBoardCallRepository.cs` (added `GetActiveCallByAcsRoomIdAsync`, `GetParticipantAsync`, `AddParticipantAsync`, `UpdateParticipant`)
- `src/Backend/Persistence/Repositories/BoardCallRepository.cs` (implemented the above)
- `src/Backend/Application/Interfaces/Repositories/IUserRepository.cs` (added `GetByAcsCommunicationUserIdAsync`)
- `src/Backend/Persistence/Repositories/UserRepository.cs` (implemented the above)

**Added (Task 3 patches, this session):**
- `src/Backend/Domain/Enums/CallParticipantRole.cs` (moved from `Application/Common/Enums/`)
- `src/Backend/Infrastructure/Boards/Calls/AcsCallService.cs` (moved from `Infrastructure/BoardCalls/`, plus patches 1/4/5/7 applied)

**Removed (Task 3 patches, this session):**
- `src/Backend/Application/Common/Enums/CallParticipantRole.cs`
- `src/Backend/Infrastructure/BoardCalls/AcsCallService.cs`

**Modified (Task 3 patches, this session):**
- `src/Backend/Infrastructure/DI/Modules/AcsModule.cs` (namespace `using` update + whitespace connection-string check)
- `src/Backend/Application/Features/BoardCalls/Commands/StartBoardCallCommand.cs` (reordered identity provisioning before the single `SaveChangesAsync`; `using` update)
- `src/Backend/Application/Features/BoardCalls/Commands/JoinBoardCallCommand.cs` (added `IUnitOfWork` + explicit `SaveChangesAsync`; `using` update)
- `src/Backend/Application/Interfaces/Services/IAcsCallService.cs` (`using` update for `CallParticipantRole`'s new namespace)

**Added (Task 4, this session):**
- `src/Backend/Application/Interfaces/Services/IBoardCallLifecycleService.cs`
- `src/Backend/Application/Services/BoardCallLifecycleService.cs`
- `src/Backend/Application/Common/Mappings/BoardCallMappings.cs`
- `src/Backend/Application/Features/BoardCalls/Commands/StartBoardCallCommand.cs`
- `src/Backend/Application/Features/BoardCalls/Commands/JoinBoardCallCommand.cs`
- `src/Backend/Application/Features/BoardCalls/Commands/LeaveBoardCallCommand.cs`
- `src/Backend/Application/Features/BoardCalls/Commands/EndBoardCallCommand.cs`
- `src/Backend/Application/Features/BoardCalls/Queries/GetActiveBoardCallQuery.cs`
- `src/Shared/Contracts/DTOs/BoardCalls/StartOrJoinBoardCallResponse.cs`

**Modified (Task 4, this session):**
- `src/Backend/Domain/Authorization/BoardRolePermissions.cs` (added `CanStartCall`)
- `src/Backend/Application/Interfaces/Services/IBoardAccessService.cs` (added `EnsureCanStartCallAsync`)
- `src/Backend/Application/Services/BoardAccessService.cs` (implemented `EnsureCanStartCallAsync`)
- `src/Backend/Domain/Constants/BoardCallConstants.cs` (added `MaxParticipants`)
- `src/Backend/Application/DI/ApplicationServiceCollectionExtensions.cs` (registered `IBoardCallLifecycleService`)

**Added (Task 3, this session):**
- `src/Backend/Application/Common/Enums/CallParticipantRole.cs`
- `src/Backend/Application/Interfaces/Services/IAcsCallService.cs`
- `src/Backend/Infrastructure/BoardCalls/AcsCallService.cs`
- `src/Backend/Infrastructure/DI/Modules/AcsModule.cs`

**Modified (Task 3, this session):**
- `src/Backend/Infrastructure/Infrastructure.csproj` (added `Azure.Communication.Identity` 1.3.1, `Azure.Communication.Rooms` 1.2.0)
- `src/Backend/Domain/Constants/ConnectionStrings.cs` (added `AzureCommunicationServices`)
- `src/Backend/Infrastructure/DI/InfrastructureServiceCollectionExtensions.cs` (wired `AddAcsModule`)
- `src/Backend/Presentation/appsettings.json` (added `AzureCommunicationServices` connection string)
- `docker-compose.yml` (added `ConnectionStrings__AzureCommunicationServices` to the `api` service)
- `.env.example` (added `AZURE_COMMUNICATION_SERVICES_CONNECTION` placeholder)

**Added (code review fixes, this session):**
- `src/Backend/Domain/Constants/BoardCallConstants.cs`
- `_bmad-output/implementation-artifacts/deferred-work.md`

**Modified (code review fixes, this session):**
- `src/Backend/Persistence/Configurations/BoardCallConfiguration.cs` (constant reference, `Cascade`, 2 `CHECK` constraints)
- `src/Backend/Persistence/Configurations/BoardCallParticipantConfiguration.cs` (`Cascade`, 1 `CHECK` constraint)
- `src/Backend/Persistence/Configurations/UserConfiguration.cs` (constant reference, 1 `CHECK` constraint)
- `src/Backend/Database/Scripts/0020_CreateBoardCallTables.sql` (amended again — `CASCADE` FKs, 3 `CHECK` constraints, `UX_`→`IX_` renames)
- `src/Backend/Database/Scripts/0021_AddAcsCommunicationUserIdToUsers.sql` (1 `CHECK` constraint, `UX_`→`IX_` rename)
- `src/Backend/Persistence/Repositories/BoardRepository.cs` (`SoftDeleteCascadeAsync` extended for `BoardCalls`/`BoardCallParticipants`)
- `src/Shared/Contracts/Notifications/BoardActions/Payloads/CallStartedPayload.cs` (now carries `BoardCallDto`)
- `src/Shared/Contracts/Notifications/BoardActions/Payloads/CallParticipantsChangedPayload.cs` (now carries `IReadOnlyList<BoardCallParticipantDto>`)

**Added (ACS pivot, prior session):**
- `src/Backend/Database/Scripts/0021_AddAcsCommunicationUserIdToUsers.sql`
- `src/Shared/Contracts/DTOs/BoardCalls/AcsCallCredentialsDto.cs`

**Modified (ACS pivot, prior session):**
- `src/Backend/Domain/Entities/BoardCall.cs` (added `AcsRoomId`)
- `src/Backend/Domain/Entities/User.cs` (added `AcsCommunicationUserId`)
- `src/Backend/Persistence/Configurations/BoardCallConfiguration.cs` (`AcsRoomId` mapping + unique index)
- `src/Backend/Persistence/Configurations/UserConfiguration.cs` (`AcsCommunicationUserId` mapping + partial unique index)
- `src/Backend/Database/Scripts/0020_CreateBoardCallTables.sql` (amended in place — added `AcsRoomId` column + unique index)

**Added (prior session, Tasks 1–2 initial implementation, unaffected by this session):**
- `src/Backend/Domain/Entities/BoardCall.cs` (base shape)
- `src/Backend/Domain/Entities/BoardCallParticipant.cs`
- `src/Backend/Persistence/Configurations/BoardCallConfiguration.cs` (base shape)
- `src/Backend/Persistence/Configurations/BoardCallParticipantConfiguration.cs`
- `src/Backend/Database/Scripts/0020_CreateBoardCallTables.sql` (base shape)
- `src/Backend/Application/Interfaces/Repositories/IBoardCallRepository.cs`
- `src/Backend/Persistence/Repositories/BoardCallRepository.cs`
- `src/Shared/Contracts/DTOs/BoardCalls/BoardCallDto.cs`
- `src/Shared/Contracts/DTOs/BoardCalls/BoardCallParticipantDto.cs`
- `src/Shared/Contracts/Notifications/BoardActions/Payloads/CallStartedPayload.cs`
- `src/Shared/Contracts/Notifications/BoardActions/Payloads/CallParticipantsChangedPayload.cs`
- `src/Shared/Contracts/Notifications/BoardActions/Payloads/CallEndedPayload.cs`

**Modified (prior session):**
- `src/Backend/Persistence/Contexts/TaskTrackerDbContext.cs`
- `src/Backend/Persistence/DI/Modules/RepositoriesModule.cs`
- `src/Shared/Contracts/Notifications/BoardActions/BoardActionNotificationType.cs`
